# CodeRabbit-Style Review — PR #538

> fix(build): remove orphaned product_api_bridge so dart analyze lib is error-free (#532)

## Walkthrough

A clean, minimal fix for #532: it deletes the orphaned generated `lib/src/api/bridges/product_api_bridge.dart` (a bridge for a non-existent `Product` feature whose broken imports produced 4 analyzer errors and broke `BuildCommand.verifyAnalyzeOrFail`), removes the now-stale `registerProductApiBridge();` example from the `api_bridge.dart` doc comment, and adds TDD/verification artifacts under `specs/bug-532-build-verifyanalyze/`. I verified the deleted file has **no live Dart references** in the tree (only its own body and the doc comment being removed) — so the deletion is safe and won't break any import/export.

| Layer / File(s) | Summary |
| --- | --- |
| `lib/src/api/bridges/product_api_bridge.dart` | Deleted (orphaned generated bridge for a non-existent `Product` feature). |
| `lib/src/core/api_bridge.dart` | Stale `registerProductApiBridge();` doc example removed. |
| `specs/bug-532-build-verifyanalyze/**` | spec.md, tdd/cycle-log.md, tdd/test-list.md, tdd/verification.md. |

**Estimated review effort:** 1 (trivial — deletion + doc cleanup + docs; no logic change).

### Pre-merge checks

- [ ] Coordinate with PR #539 so the #532 change lives in exactly **one** PR (see finding #1).
- [ ] `dart analyze lib` → no `error` lines.
- [ ] `dart test test/commands/build_command_unit_test.dart` → 40/40.

> 🐇 A phantom bridge with no Product near,
> deleted at last, the errors clear.
> But two PRs carry the selfsame delete —
> the rabbit asks, "which one should we keep?"

---

## Actionable comments

### 1. 📐 Maintainability & Code Quality · 🟠 Major · 🔨 Medium
**This PR's change is duplicated by open PR #539 (merge-conflict risk)** — `lib/src/api/bridges/product_api_bridge.dart`, `specs/bug-532-build-verifyanalyze/**`

PR #539 bundles this exact #532 fix (same file deletion + same `specs/bug-532-build-verifyanalyze/` directory) *undocumented* inside its `#533/#534/#536` change. Two open PRs now delete the same file and add the same spec path, which will conflict on merge and creates ambiguous ownership of the #532 fix.

**Recommendation:** keep **this** PR (#538) as the canonical, correctly-labeled home for #532, and have #539 drop its bundled copy (per the finding raised on #539). Whichever lands first, the other must be rebased/cleaned so the path is added/removed only once.

🤖 **Prompt for AI Agents**
```
Two open PRs (#538 and #539) both delete lib/src/api/bridges/product_api_bridge.dart
and add specs/bug-532-build-verifyanalyze/. Make the #532 change exist in exactly one
PR. Since #538 is the dedicated, labeled #532 fix, remove the bundled copy from #539
and update #539's body. Ensure no two PRs delete the same file / add the same spec path.
```

---

### 2. 🟡 Minor · 🧪 Tests / Quality
**`dart analyze lib` still emits 12 warnings/infos after the fix** — `lib` (whole package)

The PR correctly targets *errors* only and the test tolerates warnings/infos by design. But 12 residual analyzer findings remain in `lib`, which is why `verifyAnalyzeOrFail` can only assert "no errors," not "clean." Consider a follow-up ticket to silence the remaining warnings/infos so `dart analyze lib` is fully green — this would let the build gate tighten over time.

---

### 3. 🔵 Trivial
`specs/bug-532-build-verifyanalyze/tdd/cycle-log.md` still contains the unfilled placeholder `**Commit**: <filled after commit>`. Trivial doc cleanup — fill it or drop the line.

---

## Review status

**Actionable comments posted: 3** (1 Major, 1 Minor, 1 Trivial).

**Prompt for all review comments**
```
Review PR #538. (1) Coordinate with PR #539 so the #532 deletion/specs live in only
one PR (this one). (2) Consider a follow-up to clear the remaining 12 analyzer
warnings/infos in lib. (3) Fill or remove the `<filled after commit>` placeholder in
the cycle log.
```

**Verdict:** Correct, minimal, and well-evidenced fix for #532 (the TDD cycle log and mutant check are solid, and I confirmed the deletion has no live references). The only thing blocking a clean merge is the overlap with #539 — resolve that and this is good to go.
