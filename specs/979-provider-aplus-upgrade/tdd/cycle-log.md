# TDD Cycle Log: 979 — provider A+ upgrade

## Cycle 1 — RED (all spec seams, failing-first)

Tests written first under `test/plugins/provider/`:

- `provider_schema_grammar_parity_test.dart` (5 tests)
- `provider_receipt_test.dart` (4 tests)
- `provider_verify_test.dart` (6 tests)
- `provider_manifest_verify_test.dart` (3 tests)
- `provider_append_inject_test.dart` (2 tests)
- `provider_make_postpass_test.dart` (4 tests)

RED evidence (dart test, master @ 77e69f24 + tests only):

- **Dead parent flags (order 3):** parity test fails —
  `Expected: empty / Actual: Set:['domain', 'params', 'returns', 'type', 'data', 'init']`
  — the exact #876 family the spec names, still registered on
  `ProviderCommand` and never read in `run()`.
- **--init unreachable:** `Expected: true / Actual: <false>` —
  `zfa provider create --init` cannot parse (init missing from the create
  capability inputSchema).
- **type enum drift:** `Expected: ['sync', 'stream', 'completable',
  'usecase'] / Actual: <null>`.
- **configSchema drift:** `Actual: Set:['domain', 'params', 'returns',
  'type', 'init']` missing from configSchema.
- **Provenance (order 1):** `provider_receipt_test.dart` fails to load —
  `ProviderReceiptWriter` undefined (no receipt module exists); no
  `// GENERATED by zfa provider` header in emitted files.
- **Stub/conformance gates (orders 2+4):** `provider_verify_test.dart`
  fails to load — `ProviderVerifyCommand` undefined (`zfa provider verify`
  does not exist).
- **Certification (order 3):** `provider_manifest_verify_test.dart` —
  `zfa manifest --verify` not a flag (usage-error exit path), so the
  dead-flag probe never certifies.
- **Append/inject (order 5):** header assertions fail — generated
  providers carry no provenance header for the append engine to preserve.
- **Make post-pass (orders 2+4):** stub-escape findings never fire —
  `zfa make` ships stub providers silently (exit 0, no notice, no
  receipt).

## Cycle 2 — GREEN

Implementation (see plan.md Files touched):

- `ReceiptStore.saveNamed` — deterministic receipt file names
  (route-branch pattern brought to master for provider).
- `ProviderBuilder` — `// GENERATED by zfa provider — DO NOT EDIT` header
  on the fresh emission path; append path preserves it.
- `ProviderReceiptWriter` — `.zfa/receipts/provider-<Entity>.json`
  (proof.v1 + interface/methods/stubCount), emitted by the create
  capability and the make post-pass.
- `ProviderVerifier` — AST stub gate (UnimplementedError construction in
  method bodies) + conformance gate (interface methods ⊆ provider
  methods).
- `zfa provider verify <Entity>` — exit 0/1, `--> fix:` lines, `--json`
  verdict envelope.
- `ProviderCommand` — all six plugin-specific parent flags deleted;
  `verify` registered as a manual subcommand.
- `CreateProviderCapability` — `init` + `type` enum wired into
  inputSchema; receipt emission with injectable project root.
- `ProviderPlugin.configSchema` — domain/params/returns/type/init/data
  knobs (schema ≡ grammar); make context wiring for the knobs.
- `PluginCommand.consumedParentFlags` + `DataSourceCommand` declaration +
  `zfa manifest --verify [plugins...]` certification.
- `MakeCommand` post-pass — provider receipt + stub-escape/conformance
  gates (fresh stubs: visibility notice + receipt, exit 0; committed
  stubs or conformance miss: exit 1 + fix lines).

GREEN evidence: recorded in verification.md (all 24 new tests green;
full fast suite green; analyze/format clean).

## Cycle 3 — refactor

None required (orders say optional): the implementation followed the
sibling A+ module shapes directly; no post-green restructuring needed.
