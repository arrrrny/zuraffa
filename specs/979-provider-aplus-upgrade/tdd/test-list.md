# TDD Test List: 979 — provider A+ upgrade

Derived from spec.md orders 1-5. Every behavior gets a failing test before
its implementation (red → green), driven through the public seams
(capability execute, command run, builder generate).

| # | Behavior | Test file | Red evidence |
|---|----------|-----------|--------------|
| B1 | Generated provider file carries `// GENERATED by zfa provider` header | provider_receipt_test.dart | no header on disk |
| B2 | `zfa provider create` writes `.zfa/receipts/provider-<Entity>.json` with interface, methods, stubCount + proof.v1 digests | provider_receipt_test.dart | no receipt file |
| B3 | Proof check green on fresh generation, red on hand-edit | provider_receipt_test.dart | no coverage (provider artifacts unprovenanced) |
| B4 | Stub gate positive: provider with `UnimplementedError` bodies → verify exit 1 + `--> fix:` naming file + method | provider_verify_test.dart | no verify command (exit 64 usage) |
| B5 | Stub gate negative: filled bodies → verify exit 0 | provider_verify_test.dart | same |
| B6 | Conformance positive: provider missing an interface method → verify exit 1 + `--> fix:` | provider_verify_test.dart | same |
| B7 | Conformance negative: full mirror → verify exit 0 | provider_verify_test.dart | same |
| B8 | No plugin-specific parent flags on `zfa provider` (dead-flag purge, grep-provable) | provider_schema_grammar_parity_test.dart | parent registers 6 dead flags |
| B9 | `--init` parses and flows (`zfa provider create --init` → lifecycle members) | provider_schema_grammar_parity_test.dart | init missing from inputSchema → parse error |
| B10 | configSchema ≡ create inputSchema knobs (both directions) | provider_schema_grammar_parity_test.dart | configSchema only advertises data |
| B11 | `zfa manifest --verify provider` certifies green; dead flag → finding | provider_manifest_verify_test.dart | no --verify flag (usage error) |
| B12 | Make post-pass: fresh provider stubs → visibility notice, exit 0; committed provider with surviving stubs → exit 1 + fix | provider_make_postpass_test.dart | no hook — stubs ship silently |
| B13 | Make post-pass conformance miss → exit 1 + fix | provider_make_postpass_test.dart | same |
| B14 | Append round-trip: append onto generated provider preserves header + hand-written members, idempotent | provider_append_inject_test.dart | append drops nothing today, but header + receipt untested |
| B15 | Inject round-trip: `zfa provider inject` onto generated provider | provider_append_inject_test.dart | same |
