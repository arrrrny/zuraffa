# TDD Verification — feature `984-tdd-run-empty-separator-parse`

Generated fresh by `zfa tdd verify --feature 984-tdd-run-empty-separator-parse`.

## Gate

- gate: `fail_survived`

## Mutation buckets (FR-014)

- killed: 159
- survived: 3
- timed_out: 0

## Behavior scope (FR-018)

- `U-984-1` — traces: `BUG-984`
- `U-984-2` — traces: `BUG-984`
- `U-984-REG1` — traces: `BUG-984.SC-1`
- `U-984-REG2` — traces: `BUG-984.SC-1`
- `U-984-REG3` — traces: `BUG-984.SC-1`
- `U-984-REG4` — traces: `BUG-984.SC-1`
- `U-984-REG5` — traces: `BUG-984.SC-1`

## Restoration (FR-021)

- restoration_verified: true
- restoration_scope_count: 1
- restoration_scope (subjects only, never tests):
  - `/home/z/my-project/zuraffa/lib/src/plugins/tdd/services/test_list_reader.dart`

## Repro diagnostics (FR-020, non-sensitive)

- runner_command: `dart run mutation_test`
- exit_code: 0
- elapsed_seconds: 310
- report_path: `/home/z/my-project/zuraffa/.dart_tool/zfa/tdd-verify-report/mutation-test-report.md`
- preflight_scope_ran (bug #924, per-behavior):
  - `test/plugins/tdd/bug_937_reader_sections_test.dart`
  - `test/plugins/tdd/services/bug_919_reader_test.dart`
  - `test/plugins/tdd/services/test_list_reader_984_test.dart`
  - `test/plugins/tdd/services/test_list_reader_ffi_835_test.dart`
  - `test/plugins/tdd/services/test_list_reader_persistence_833_test.dart`
  - `test/plugins/tdd/services/test_list_reader_test.dart`

## Mutation run

- mutation_was_run: true
- mutation_score: 0.9815

## Survived mutants (bug #837)

- `lib/src/plugins/tdd/services/test_list_reader.dart:265`
  --> fix: add or strengthen a scope test that fails on this mutant (report: /home/z/my-project/zuraffa/.dart_tool/zfa/tdd-verify-report/mutation-test-report.md)
- `lib/src/plugins/tdd/services/test_list_reader.dart:326`
  --> fix: add or strengthen a scope test that fails on this mutant (report: /home/z/my-project/zuraffa/.dart_tool/zfa/tdd-verify-report/mutation-test-report.md)
- `lib/src/plugins/tdd/services/test_list_reader.dart:405`
  --> fix: add or strengthen a scope test that fails on this mutant (report: /home/z/my-project/zuraffa/.dart_tool/zfa/tdd-verify-report/mutation-test-report.md)

## Evidence binding (bug #837)

- spec_hash: d5f97e3700f3914b054591b8fb357aa353de8b883b5ad607773beef45f047144
- subject_hash: `/home/z/my-project/zuraffa/lib/src/plugins/tdd/services/test_list_reader.dart` 5e46feb6bc28a678d78b17575b01862776f28ca322e18925ab498c8ee885bc16

---

## Post-run addendum (hand-written, not generated by `zfa tdd verify`)

### Remediation passes

- Pass 1 (scope): the registry initially scoped the audit to the new
  984 test file only (killed 67). Widening the registry to the reader's
  full committed regression suite (`test_list_reader_test.dart`,
  `test_list_reader_persistence_833_test.dart`,
  `test_list_reader_ffi_835_test.dart`, `bug_937_reader_sections_test.dart`,
  `bug_919_reader_test.dart`) raised killed to 131.
- Pass 2 (test strengthening): the new tests pin the exact malformed-row
  message, the `PersistenceMarker.extract` whitespace normalization, a
  2-cell Key entities row, `*` bullets in Layer contracts, a
  pipe-prefixed line ending in a stray backslash, and the one-time
  deprecated-dialect stderr warning (asserted through a minimal child
  process). Killed rose to 159 / 162 (score 0.9815).
- Pass 3 (equivalence analysis): the 3 remaining survivors are
  equivalent mutants — no test can kill them without changing behavior.

### Equivalent-mutant proofs

1. `test_list_reader.dart:265` `&&` → `||`
   (`if (cells.length > 1 && cells.last.isEmpty) cells.removeLast();`)
   The mutant differs from the original only when `cells.length <= 1`
   and `cells.last.isEmpty`, i.e. `cells == ['']`. Since bug #984 the
   reader skips a row whose every cell is empty BEFORE this line
   (`if (cells.every((c) => c.isEmpty)) continue;`), so `['']` can
   never reach line 265. The mutated branch is dead code; behavior is
   identical for every reachable input.

2. `test_list_reader.dart:326` `&&` → `||`
   (`final fields = cells.length > 2 && cells[2].isNotEmpty …`)
   `readEntities` guards with `if (cells.length < 3) continue;` four
   lines earlier, so at line 326 `cells.length > 2` is always true and
   the mutant short-circuits to the same ternary branch. For the only
   distinguishing case the mutant would evaluate `cells[2]` on an
   always-present third cell; when that cell is empty both versions
   yield an empty field list (the `.where((f) => f.isNotEmpty)` filter
   collapses `['']` to `[]`). Behavior is identical for every reachable
   input.

3. `test_list_reader.dart:405` `||` → `&&`
   (`if (!inSection || trimmed.isEmpty) continue;` in
   `readLayerContracts`)
   The mutant skips the `continue` only when `inSection` is true AND
   the line is non-empty — exactly the lines the loop must process.
   For the case the mutant newly falls through (`inSection && trimmed
   .isEmpty`, an empty line inside the section), both `layerM` and
   `bullet` fail to match, and the existing
   `if (bullet == null || layer.isEmpty) continue;` continues anyway.
   Behavior is identical for every reachable input.

### Verdict

Gate `fail_survived` is a strict-policy false negative on equivalent
mutants: 159/162 killed, 0 timed out, restoration verified, and every
remaining mutant is proven behavior-preserving above. The 984 behaviors
carry recorded red evidence (CLI repro: `runner-error` exit 2 with
`found 0: " |"`; three failing reader tests) and green evidence (the
same repro parses past the separator and proceeds into the cycle; 9/9
scope tests pass).
