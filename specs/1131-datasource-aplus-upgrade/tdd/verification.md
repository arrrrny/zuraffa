# TDD Verification: 1131 — datasource A+ upgrade

**Verdict: GREEN — all five orders implemented and tested (red → green),
all four acceptance criteria PROVED by executed runs.**

Method: every behavior got a failing test FIRST (cycle-log.md records the
red evidence: the missing `DataSourceVerifyCommand` module, the capability's
missing `projectRoot`/receipt plumbing, hostile-arg exceptions escaping
`execute()`/`plan()`, and emissions starting with the bare
`// GENERATED - DO NOT EDIT` marker instead of a provenance header), then
the implementation, then the full verification battery below. No step below
was claimed without being run in this session (Dart 3.13.3 stable, Linux
x64, cloud sandbox).

## 1. Orders → implementation

| Order | Implementation | Proved by |
|---|---|---|
| 1 — `zfa datasource verify <Entity>` | `DatasourceVerifier` (AST: entity parse + field extraction, interface method signatures, first-type-argument conformance of `UpdateParams`/`DeleteParams`/`ToggleParams` vs the entity's declared id-field type, entity-type reference conformance for get/getList/list/create/update/toggle/watch/watchList); `DataSourceVerifyCommand` registered manually on `DataSourceCommand`; exit 0/1/2 with `--> fix:` lines; `--json` → single canonical `verdict.v1` envelope (`VerdictEnvelope`, issue #1105) with `subject {kind: datasource, entity}` and `findings [{kind, file, member, fix}]` | `datasource_verify_test.dart` (10 tests: clean pass, id-field drift, missing interface, missing entity, malformed entity, usage exit, json pass shape, json drift findings, json malformed fail, subcommand registration), real-CLI demo §3 |
| 2 — capability receipts | `DatasourceReceiptWriter` (mirrors spec-979 `ProviderReceiptWriter`): deterministic `.zfa/receipts/datasource-<entity>.json` via `ReceiptStore.saveNamed` with `interface_sha256` + `entity_source {path, sha256, bytes}` extras; persisted INSIDE `CreateDataSourceCapability.execute()` on every successful non-dry-run generation, so both the `zfa datasource create` subcommand and the standalone positional path ship it; the positional command's `_emitReceipt` was rerouted through the same writer (same document, plus methodset/runHash) | `datasource_capability_receipt_test.dart` (5 tests: binding correctness vs on-disk bytes, entity-hash binding, `ProofChecker` green, dry-run writes none, declined run writes none), real-CLI demo §3 |
| 3 — `--explain` | `DatasourceExplainer`: datasource types (remote/local with will-be-generated/exists/not-requested state; mock/sqlite delegated to `zfa mock create` / `zfa sqlite create`), entity field→method mapping (id-field → UpdateParams/DeleteParams/ToggleParams wiring, query-field → get/watch, others flow through the entity type), interface shape (signatures mirroring `DataSourceInterfaceBuilder`'s method table). Wired as a schema-declared `explain` property (subcommand `--explain`) AND a parent flag on `DataSourceCommand` (+ `consumedParentFlags`); `CapabilityCommand` prints `data['explain']` on the read-only path | `datasource_explain_test.dart` (5 tests: read-only, field mapping, interface shape, schema-declared flag, parent path), real-CLI demo §3 |
| 4 — error handling | FULL generate() path wrapped: `CreateDataSourceCapability.execute()` builds config INSIDE the try (missing/`String`-where-`List` args → `ExecutionResult(success: false)` with reason + fix hint); `plan()` returns an invalid, message-carrying `EffectReport` instead of throwing; the verify gate parses defensively (malformed entity = finding, never a crash) | `datasource_error_handling_test.dart` (5 tests: missing name, methods-as-String, throwing generator, hostile plan args, malformed entity on disk), real-CLI demo §3 |
| 5 — provenance headers | `DatasourceProvenance.headerFor(entity, variant)` — `// GENERATED by zfa datasource — DO NOT EDIT BY HAND.` + regeneration hint — stamped on every fresh emission in `interface_generator.dart`, `remote_generator.dart`, `local_generator.dart` (above the spec-library markers, dart_style-stable); append/merge paths untouched (the AST engine preserves the leading block) | `datasource_provenance_test.dart` (4 tests: all three file kinds carry header + hint, variant naming, emission validity, append preserves header), real-CLI demo §3 |

## 2. Test runs (ACTUAL counts)

- **New datasource suite**: `dart test test/plugins/datasource/` →
  **+70 All tests passed** (29 new across five files + 41 pre-existing,
  incl. the spec-977 receipts suite and the #769 zero-files guard).
- **`dart test test/commands`** (the capability/manifest/exit-protocol
  surface `CapabilityCommand` lives in): **+286 All tests passed.**
- **Fast tier, chunked with kernel-cache cleanup between chunks** (per the
  repo's cloud-agent guidance in `dart_test.yaml`/`tools/run_tests_chunked.sh`):
  - test/core + test/regression: **611 passed** (1 skipped, pre-existing)
  - test/cli + test/config: **210 passed**
  - test/agent 233, test/graphql 192, test/simulation 192, test/tdd 128,
    test/dda 59, test/domain 62, test/skin 84, test/zap 76, test/engine 45,
    test/package_sdk 42, test/state 68, test/utils+logging 105,
    test/i18n 11, test/migration 20, test/property 9, test/secure_storage 11,
    test/session 20, test/share 5, test/skew 29, test/app_update 6,
    test/biometrics 7, test/clipboard 6, test/device 6, test/feature_flags 54
  - test/plugins: api 30, app_shell 83, benchmark 59, cache 29, cli 3,
    di 46, feature 13, graphql 6, gym 40, method_append+module 6,
    provider+repository+route 173, service+shadcn+skin_contract 101,
    slice+strategy 177, sync+helpers+misc 37, state 21, xray+mcp 213,
    skeleton+sqlite+mock (in the 537-test combined run) — **all passed**
  - test/plugins/tdd fast-tier files: **37/59 files green** (all their
    tests); the other 22 are `slow`-tagged whole-file suites (make/run/
    runner/verify pipelines) — excluded by the default `exclude_tags: slow`
    tier, they report "No tests ran" under it, which is the repo's own
    cloud-agent contract, not a failure.
  - test/benchmark + test/helpers: **9 passed**;
    datasource self-hosting: **5 passed**.
- **Environment-limited (pre-existing, NOT caused by this change)**:
  `downstream_compile_gate_test`, `controller_compile_test`,
  `view_compile_test` — their `setUpAll` shells out to `flutter pub get`;
  no Flutter SDK exists in this sandbox. These are the flutter-cluster
  tests the chunked runner drops on cloud agents. Slow tier
  (`--preset=all`) not run per the repo's own dart_test.yaml warning
  (fills several GB under /tmp on disposable agents; two disk-full events
  were hit and recovered during chunking — kernel caches cleaned after
  every chunk, smoke fixtures removed).
- **`dart analyze $(git diff --name-only HEAD -- '*.dart')`**: No issues
  found.
- **`dart analyze lib test`**: **104 issues — identical to the clean-master
  baseline (104, all pre-existing infos), 0 errors/warnings.**
- **`dart format`**: applied to the touched files; `dart format .` also
  flagged two files with PRE-EXISTING master formatting drift
  (`cli_plugin.dart`, `cli_phantom_regression_test.dart`) — both were
  REVERTED to keep this PR scoped.

## 3. Real-CLI demo (temp project, smoke1131)

- `zfa datasource create Product --force` → remote + interface emitted;
  both start with the provenance header; `.zfa/receipts/datasource-product.json`
  contains `schema: proof.v1`, `interface_sha256` (7397866565398c3a…) and
  `entity_source {path: lib/src/domain/entities/product/product.dart,
  sha256: 8b00f7b8c2026377…}`; `zfa proof check` → **3 receipt(s),
  2 artifact(s) verified, 0 finding(s) — OK**.
- `zfa datasource verify Product` (clean) → exit 0, "✅ … id-field `id`:
  String"; `--json` → one-line `verdict.v1` envelope, `verdict: pass`.
- Drift (entity id changed to `int` vs String-typed interface, regenerated
  via `--json '{"methods":["get","update","delete"],"force":true}'`) →
  **exit 1** with `[id-field type drift] update/delete` findings and two
  `--> fix: regenerate … --id-field-type int` lines.
- Malformed entity (`class Product { final String id`) → **exit 1**,
  `[malformed entity source]` + `--> fix: repair the syntax errors…` —
  no uncaught exception, no stack trace.
- `zfa datasource create Product --explain` → plan text listing remote/
  local/mock/sqlite types, `String id — id-field … UpdateParams/
  DeleteParams/ToggleParams` mapping, `Interface shape: ProductDataSource`
  — nothing generated.

## 4. Generation-semantics constraint — HELD

Clean-master A/B: the same `zfa datasource create Product --methods …`
invocation on stashed (master) sources produces the same emission shape as
this branch (the subcommand flag path wires no methodset either way —
methods flow via `--json`/make input, unchanged here). No generation
semantics were altered; receipts/explain/headers are additive.

## 5. Acceptance criteria — PROVED

- **AC-1** `zfa datasource verify <Entity>` reports conformance or drift —
  PROVED (test battery + real-CLI clean/drift/malformed runs with exit
  codes 0/1 and `--> fix:` lines).
- **AC-2** `.zfa/receipts/datasource-<entity>.json` exists after create —
  PROVED (test + real CLI on both invocation paths; `zfa proof check` green).
- **AC-3** All generated datasource files contain the provenance header —
  PROVED (interface, remote, local — fresh emissions stamped, append
  preserves).
- **AC-4** A malformed entity does not crash with uncaught exception —
  PROVED (capability path: honest `success:false`; verify path: exit 1 +
  finding; real CLI: clean exit 1, no stack trace).
