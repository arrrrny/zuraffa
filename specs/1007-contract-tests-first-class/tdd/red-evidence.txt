RED EVIDENCE (spec 1007, 2026-09-05):
- plan_contract_kind_1007_test.dart: 4 of 5 fail — plan emits NO contract:<id> rows (engine plan lacks | contract:A1 |; reader resolves no contract kind; re-plan preserves nothing). The 1 passing test is the additive baseline (a spec without Layer Contracts plans contract-free) — the hard constraint holds BEFORE the change.
- gen_contract_kind_1007_test.dart: 3 of 3 fail — zfa tdd gen contract:A1 is "unknown behavior id" (no contract kind in any surface).
- verify_red_contract_blocked_1007_test.dart: 2 of 4 fail — a deliberately unimplemented method certifies an ordinary assertion RED (classification=assertion, certified=true) instead of BLOCKED; no contract-blocked receipt; the run stop reports result=stopped instead of result=blocked. The 2 passing tests are the additive baselines (a NON-contract behavior with the same transcript still certifies red; StepRunner passes the classification token through).
- contract_severity_1007_test.dart: model-level tests green after the minimal compile surface (GapSeverity/severity/ranking/forStop — trivial pure model); the behavioral gap (corpus run stamping + status printing) has no direct test yet.
