00:00 +0: loading test/plugins/tdd/commands/spec_1142_red_repro_test.dart
00:00 +0: RED #1142: declared slots (mobile, macos) must emit the AdaptiveViewState skeleton with per-platform layout stubs — today a single-layout Column is generated instead
00:00 +0 -1: RED #1142: declared slots (mobile, macos) must emit the AdaptiveViewState skeleton with per-platform layout stubs — today a single-layout Column is generated instead [E]
  Expected: contains 'class A001View extends StatefulWidget {'
    Actual: '// GENERATED STUB — `zfa tdd gen A-001` (spec 044-test-tdd-generation).\n'
              'library;\n'
              '\n'
              'import \'package:flutter/material.dart\';\n'
              '\n'
              '/// View-builder subject for behavior A-001 (issue #939): returns\n'
              '/// the deterministic minimal view composed from the declared Presentation\n'
              '/// layer contract and the scenario assertions of the behavior description\n'
              '/// (issue #964 finder-kind taxonomy: presence literals render as Text,\n'
              '/// route/enabled-state literals as a labeled affordance, absence literals\n'
              '/// render nothing). Scenario-specific behavior (navigation, validation,\n'
              '/// state) is the sanctioned handcraft seam — implement it in [A001View].\n'
              'Widget subject_a_001() => A001View();\n'
              '\n'
              '/// The minimal view for behavior A-001 (issue #939 skeleton).\n'
              '///\n'
              '/// the login page shows \'Welcome back\' with a sign in button\n'
              'class A001View extends StatelessWidget {\n'
              '  const A001View({super.key});\n'
              '\n'
              '  @override\n'
              '  Widget build(BuildContext context) {\n'
              '    return Column(\n'
              '      mainAxisSize: MainAxisSize.min,\n'
              '      children: <Widget>[\n'
              '            Text(\'Welcome back\'),\n'
              '            TextField(),\n'
              '            ElevatedButton(onPressed: () {}, child: Text(\'ShadButton\')),\n'
              '            Text(\'mobile\'),\n'
              '            Text(\'macos\'),\n'
              '      ],\n'
              '    );\n'
              '  }\n'
              '}\n'
              ''
     Which: does not contain 'class A001View extends StatefulWidget {'
  RED: single-layout StatelessWidget emitted instead of the AdaptiveViewState skeleton
  
  package:matcher                                               expect
  test/plugins/tdd/commands/spec_1142_red_repro_test.dart 98:5  main.<fn>
  
00:00 +0 -1: Some tests failed.

Failing tests:
  test/plugins/tdd/commands/spec_1142_red_repro_test.dart: RED #1142: declared slots (mobile, macos) must emit the AdaptiveViewState skeleton with per-platform layout stubs — today a single-layout Column is generated instead

Consider enabling the flag chain-stack-traces to receive more detailed exceptions.
For example, 'dart test --chain-stack-traces'.
