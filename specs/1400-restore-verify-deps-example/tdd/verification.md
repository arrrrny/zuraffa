# TDD Verification — Spec 1400 — Verdict: PASS (reproduction, 2026-09-10)

Certification that the committed `004-login-ui` mutation evidence is
reproducible at HEAD against the restored TDD dev_dependencies — with
the residual, unrelated U1 blocker explicitly recorded.

**Endpoint gate: `fail_survived`.** The reproduction is bit-exact, but
the verify gate itself is not green — the reproduced evidence carries 8
survivors, and the full lane additionally stops earlier at
`preflight_red` (R5).

## Runs executed (this branch, Flutter 3.47.3 / Dart 3.13.3)

| # | Command (repo root unless noted) | Result |
|---|----------------------------------|--------|
| R1 | `flutter pub get` (in `example/`) | PASS — `Got dependencies!`, zero `dependency_overrides` (SC-001) |
| R2 | `dart test test/package_sdk/bug_1369_example_tdd_baseline_test.dart` | PASS — `+1: All tests passed!` (B1, B2) |
| R3 | `flutter test test/presentation/pages/login/login_view_test.dart test/tdd/004-login-ui/a{3,4,5,6,7}_test.dart` (in `example/`) | PASS — `+8: All tests passed!` (the committed evidence's exact preflight scope) |
| R4 | `dart run mutation_test .dart_tool/zfa/tdd-verify-mutation-repro.xml -f md -o .dart_tool/zfa/tdd-verify-report-repro` (in `example/`) | PASS — exit 0, 56 mutants, see below (B3) |
| R5 | `dart run zuraffa:zfa tdd verify --feature 004-login-ui --project example --runner flutter --json` | **FAIL — `gate: preflight_red`, `mutation_was_run: false`** — blocked at the preflight by `test/tdd/004-login-ui/u1_test.dart` (`UnimplementedError: subject_u1 not implemented`, #1377's deliberate honest-red stub); the endpoint gate additionally remains `fail_survived` per the reproduced evidence. Unrelated pre-existing failure; flagged, not fixed (source-code change forbidden here) |
| R6 | `test: ^1.0.0` inserted into `example/pubspec.yaml` + `flutter pub get` | **FAIL — version solving failed** (`flutter_test from sdk is incompatible with test`, #1189/#1370 conflict class). Reverted; R1 re-proven clean after revert (B4 counter-evidence) |
| R7 | `dart format --output=none --set-exit-if-changed lib test` | PASS — 0 changed (the CI format gate scope) |
| R8 | `dart analyze` over changed `.dart` files | N/A — zero `.dart` files changed by this PR (data + docs only) |

## Mutation reproduction (R4) vs committed evidence

The committed evidence (fb44db98, regenerated by #1376) registers 6
subjects / 6 tests; that audit surface is byte-identical at HEAD —
subject for subject, hash for hash (binding table below).

| Metric | Committed evidence | Reproduction (R4) | Match |
|--------|--------------------|-------------------|-------|
| mutation_was_run | true | true (exit 0, real per-mutant report) | ✓ |
| mutants found | 56 | 56 | ✓ |
| killed | 48 | 48 | ✓ |
| survived | 8 | 8 | ✓ |
| mutation_score | 0.8571 | 0.8571 (`Undetected Mutations: 8 (14.29%)`, Quality rating: B) | ✓ |
| gate | `fail_survived` | `fail_survived` (same 8 survivors) | ✓ |
| timed_out | 0 | 0 | ✓ |
| survivors (file:line) | a4_subject:49; a7_subject:59; login_view:25, 36, 75, 232, 298, 298 | identical, line-for-line | ✓ |
| elapsed | 436s | 431s | ✓ (noise) |

### Evidence binding (bug #837) — recomputed vs recorded

The committed evidence records a sha256 for its registry plus one for
each of its 6 subjects. Recomputed at HEAD with `shasum -a 256 <path>`
run in `example/`:

| Binding | Recorded (fb44db98) | Recomputed (HEAD) | Match |
|---------|---------------------|-------------------|-------|
| subject `lib/src/presentation/pages/login/login_view.dart` | `c18ab31c029e09036edd487317a79f5631365b3b15b5692e28668f034b23c28c` | `c18ab31c029e09036edd487317a79f5631365b3b15b5692e28668f034b23c28c` | ✓ |
| subject `lib/tdd/004-login-ui/a3_subject.dart` | `0bf3fc39fdefbe8cffe10fd3fadeddc19390af5780211eeae1e567a47508de4d` | `0bf3fc39fdefbe8cffe10fd3fadeddc19390af5780211eeae1e567a47508de4d` | ✓ |
| subject `lib/tdd/004-login-ui/a4_subject.dart` | `823b158b5bffbd49cfd79ab4e30bb2f6b6bac743179decaba92d06b5c2a2639f` | `823b158b5bffbd49cfd79ab4e30bb2f6b6bac743179decaba92d06b5c2a2639f` | ✓ |
| subject `lib/tdd/004-login-ui/a5_subject.dart` | `f3026f8ee4ab3feff3991619acb26dc46d40cdff33ed2596ca3929971f57f45f` | `f3026f8ee4ab3feff3991619acb26dc46d40cdff33ed2596ca3929971f57f45f` | ✓ |
| subject `lib/tdd/004-login-ui/a6_subject.dart` | `77520b2244471e120a6e3ed55cf21d9ff77ef05ab4fe04a53e3cc0ec52110d2c` | `77520b2244471e120a6e3ed55cf21d9ff77ef05ab4fe04a53e3cc0ec52110d2c` | ✓ |
| subject `lib/tdd/004-login-ui/a7_subject.dart` | `a2d1a99d73db32e15abb7f7876616e37a3796cd8dc698f4df4cbbda2eb9abdeb` | `a2d1a99d73db32e15abb7f7876616e37a3796cd8dc698f4df4cbbda2eb9abdeb` | ✓ |
| `spec_hash` (`example/specs/004-login-ui/tdd/artifacts.json`) | `6b85c7e6f952479b86bc8b879e74ff3a2c96c5625cdc7f1bdce8cc9d31bfd957` | `265dea90109056771667f672f7883bab90bd1fcdc20c15ac6d48c750909c6624` | ✗ — expected: #1377 appended the `U1` record after the evidence was generated |

The 6 subject hashes are all equal, which is exactly what makes the R4
comparison valid: the mutated surface is the same bytes the committed
evidence audited. The registry differs for a known reason — #1377 added
the `U1` behavior record (`u1_subject.dart` / `u1_test.dart`) to
`artifacts.json`; the evidence's own 6 records (A3–A7, W1) keep their
exact subject/test bindings, and the added `U1` record is what the full
lane now trips on (R5).

Verdict for FR-3 / SC-002: **REPRODUCED BIT-EXACT** against the restored
dependency tree (`mutation_test` and `coverage` resolved FROM
`example/pubspec.yaml` dev_dependencies — the dependency the issue says
was broken).

## Kernel-cache hygiene

`rm -rf .dart_tool/test/` + `$TMPDIR/dart_test.kernel.*` before and
after the mutation run (audit protocol). Post-run `git status` of
`example/lib` + `example/test`: clean — all subjects restored
byte-identical.

## Test-first

Not a code-fix PR: the data fix pre-landed (#1369 restore, #1370
supersession) before this branch was cut, so no new red→green code
cycle exists inside the fix-only-`example/pubspec.yaml` constraint.
The recorded reds (issue-filing audit death; residual U1 preflight red;
plain-`test` solver failure) and the green certification runs are in
[cycle-log.md](./cycle-log.md) and [test-list.md](./test-list.md).

## AC coverage

- AC-1 (restore deps): FR-1 ✓ (pair present at canonical constraints,
  R2), FR-2 ✓ (plain `test` absent — R2 asserts absence; R6 proves the
  literal restore is unresolvable and would re-break the audit).
- AC-2 (verify reproducibility): FR-3 ✓ (R4 bit-exact reproduction);
  full-lane gate status honestly reported by R5 with the U1 blocker
  attributed to #1377.
- AC-3 (CI guard, optional): satisfied by existing surfaces — the
  structural pin in CI's `dart test test` lane + the
  `flutter_consumer_smoke` job (spec locked decision 5); no new CI
  surface added.

## NOT taken

- Implementing `subject_u1` (turns the full-lane gate green) — source
  code change, forbidden by this issue's hard constraints; belongs to
  the U1 lane (#1377).
- Hand-editing the flagship's generated
  `example/specs/004-login-ui/tdd/verification.md` — generated-file
  provenance stays with the verify lane; it will regenerate when U1
  lands.
