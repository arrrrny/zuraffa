# Bug Fix Spec: build verifyAnalyzeOrFail lib errors (issue #532)

- **Issue**: https://github.com/arrrrny/zuraffa/issues/532
- **Source assessment**: `.specify/bugs/build-verifyanalyzeorfail-lib-errors/assessment.md`
- **Failing test**: `test/commands/build_command_unit_test.dart:589`
  `BuildCommand helpers (in-process, #276) verifyAnalyzeOrFail (issue #415 — invalid --fatal-infos flag) runs `dart analyze lib` without the rejected --fatal-infos=value flag and reports no errors on the current (warning/info-only) lib`
- **Symptom**: `BuildCommand.verifyAnalyzeOrFail` runs `dart analyze lib` and returns `false` because `lib/src/api/bridges/product_api_bridge.dart` (marked `// GENERATED BY zfa api Product`) imports non-existent `Product`/`GetProductUseCase`, producing 2 analyzer errors. The test expects a warning/info-only lib.

## Acceptance criteria

- **AC1**: `dart analyze lib` reports no errors (warnings/infos tolerated).
- **AC2**: `test/commands/build_command_unit_test.dart` (esp. the #415 `verifyAnalyzeOrFail` test) passes.

## Out of scope

- Regenerating a real Product API bridge (Product is not an intended feature in this repo).
- Silencing the remaining warnings/infos in lib.
