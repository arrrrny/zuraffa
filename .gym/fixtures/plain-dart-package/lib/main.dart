/// Placeholder library for the plain-Dart-package fixture.
///
/// No Zuraffa deps, no entities — a package `zfa doctor` must classify as
/// not Zuraffa-compatible so the exercise can train stop-and-report.
library;

void main() {
  // Intentionally trivial: the fixture exists to be DETECTED, not rewritten.
}
