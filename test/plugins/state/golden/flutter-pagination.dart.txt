// GENERATED - DO NOT EDIT
import 'package:zuraffa_flutter/zuraffa_flutter.dart';

import '../../../domain/entities/order/order.dart';

class OrderState {
  const OrderState({
    this.error,
    this.orderList = const <Order>[],
    this.offset = 0,
    this.limit = 10,
    this.hasMore = true,
    this.order,
    this.isGetting = false,
    this.isGettingList = false,
  });

  /// The current error, if any
  final AppFailure? error;

  /// The single Order entity
  final Order? order;

  /// The list of Order entities
  final List<Order> orderList;

  /// The current offset for pagination
  final int offset;

  /// The maximum number of items to fetch
  final int limit;

  /// Whether more items are available to fetch
  final bool hasMore;

  /// Whether get is in progress
  final bool isGetting;

  /// Whether getList is in progress
  final bool isGettingList;

  OrderState copyWith({
    AppFailure? error,
    List<Order>? orderList,
    int? offset,
    int? limit,
    bool? hasMore,
    Order? order,
    bool? isGetting,
    bool? isGettingList,
  }) => OrderState(
    error: error ?? this.error,
    orderList: orderList ?? this.orderList,
    offset: offset ?? this.offset,
    limit: limit ?? this.limit,
    hasMore: hasMore ?? this.hasMore,
    order: order ?? this.order,
    isGetting: isGetting ?? this.isGetting,
    isGettingList: isGettingList ?? this.isGettingList,
  );

  bool get isLoading => isGetting || isGettingList;

  bool get hasError => error != null;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is OrderState &&
          other.error == error &&
          other.orderList == orderList &&
          other.offset == offset &&
          other.limit == limit &&
          other.hasMore == hasMore &&
          other.order == order &&
          other.isGetting == isGetting &&
          other.isGettingList == isGettingList;

  @override
  int get hashCode =>
      error.hashCode +
      orderList.hashCode +
      offset.hashCode +
      limit.hashCode +
      hasMore.hashCode +
      order.hashCode +
      isGetting.hashCode +
      isGettingList.hashCode;

  @override
  String toString() =>
      'OrderState(error: $error, orderList: $orderList, offset: $offset, limit: $limit, hasMore: $hasMore, order: $order)';
}

// END GENERATED
