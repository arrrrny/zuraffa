// Generated by zfa
// zfa generate Todo --methods=getList --test

import 'package:flutter_test/flutter_test.dart';
import 'package:mocktail/mocktail.dart';
import 'package:zuraffa/zuraffa.dart';
import 'package:zuraffa/src/domain/entities/todo/todo.dart';
import 'package:zuraffa/src/domain/repositories/todo_repository.dart';
import 'package:zuraffa/src/domain/usecases/todo/get_todo_list_usecase.dart';

class MockTodoRepository extends Mock implements TodoRepository {}
class MockTodo extends Mock implements Todo {}

void main() {
  late GetTodoListUseCase useCase;
  late MockTodoRepository mockRepository;

  setUp(() {
    registerFallbackValue(const ListQueryParams<Todo>());
    mockRepository = MockTodoRepository();
    useCase = GetTodoListUseCase(mockRepository);
  });

  group('GetTodoListUseCase', () {
    final tTodo = MockTodo();
    
        final tTodoList = [tTodo];


    test('should call repository.getList and return result', () async {
      // Arrange
      when(() => mockRepository.getList(any())).thenAnswer((_) async => tTodoList);

      // Act
      final result = await useCase(const ListQueryParams<Todo>());

      // Assert
      verify(() => mockRepository.getList(any())).called(1);
      expect(result.isSuccess, true);
      expect(result.getOrElse(() => throw Exception()), equals(tTodoList));
    });

    test('should return Failure when repository throws', () async {
      // Arrange
      final exception = Exception('Error');
      when(() => mockRepository.getList(any())).thenThrow(exception);

      // Act
      final result = await useCase(const ListQueryParams<Todo>());

      // Assert
      verify(() => mockRepository.getList(any())).called(1);
      expect(result.isFailure, true);
    });
  });
}
