// Generated by zfa
// zfa generate ParserConfig --methods=get --test

import 'package:flutter_test/flutter_test.dart';
import 'package:mocktail/mocktail.dart';
import 'package:zuraffa/zuraffa.dart';
import 'package:zuraffa/src/domain/entities/parser_config/parser_config.dart';
import 'package:zuraffa/src/domain/repositories/parser_config_repository.dart';
import 'package:zuraffa/src/domain/usecases/parser_config/get_parser_config_usecase.dart';

class MockParserConfigRepository extends Mock
    implements ParserConfigRepository {}

class MockParserConfig extends Mock implements ParserConfig {}

void main() {
  late GetParserConfigUseCase useCase;
  late MockParserConfigRepository mockRepository;

  setUp(() {
    mockRepository = MockParserConfigRepository();
    useCase = GetParserConfigUseCase(mockRepository);
  });

  group('GetParserConfigUseCase', () {
    final tParserConfig = MockParserConfig();

    test('should call repository.get and return result', () async {
      // Arrange
      when(() => mockRepository.get(any()))
          .thenAnswer((_) async => tParserConfig);

      // Act
      final result = await useCase(const QueryParams('1'));

      // Assert
      verify(() => mockRepository.get(any())).called(1);
      expect(result.isSuccess, true);
      expect(result.getOrElse(() => throw Exception()), equals(tParserConfig));
    });

    test('should return Failure when repository throws', () async {
      // Arrange
      final exception = Exception('Error');
      when(() => mockRepository.get(any())).thenThrow(exception);

      // Act
      final result = await useCase(const QueryParams('1'));

      // Assert
      verify(() => mockRepository.get(any())).called(1);
      expect(result.isFailure, true);
    });
  });
}
