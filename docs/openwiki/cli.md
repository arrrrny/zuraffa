# Openwiki CLI — the command fleet

<!-- GENERATED by `tool/generate_openwiki_cli_docs.dart` from the live dispatcher — regenerate, never hand-edit. -->

- Registry: **59 commands** (as of this generation — the count is re-emitted on every regeneration, never pinned by hand).
- Exit codes (SPEC 917): `0` success, `1` failure/drift, `2` usage error. Every non-zero exit ends with a machine-actionable `--> fix:` line.
- Machine envelopes: `--json` emits the `zuraffa.verdict.v1` document; generated artifacts carry `proof.v1` receipts under `.zfa/receipts/`.

## `zfa agent`

```
Agent runtime (shell daemon, leases, missions, budgets)

Usage: zfa agent <subcommand> [arguments]
-h, --help    Print this usage information.

Available subcommands:
  shell   Run the agent shell daemon (NDJSON over stdio) — leases, missions, budgets (#808)

Run "zfa help" to see global options.
```

## `zfa api`

```
Generate API bridge for a Zuraffa entity

Usage: zfa api <EntityName> [options]
-h, --help       Print this usage information.
-o, --output     Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                 (defaults to "lib/src")
    --dry-run    Preview generated files without writing to disk
-f, --force      Overwrite existing files
-v, --verbose    Enable detailed logging
    --revert     Revert generated files (delete them)
    --name       Entity name (alternative to the positional argument)
-d, --domain     Override the domain name segment in extension methods

Run "zfa help" to see global options.
```

## `zfa app`

```
App-level generators (shell, theme, …). Currently exposes `shell`.

Usage: zfa app <subcommand> [arguments]
-h, --help    Print this usage information.

Available subcommands:
  shell   Generate the app shell (main.dart + MyApp + app_router.dart)

Run "zfa help" to see global options.
```

## `zfa apply`

```
Execute a previously generated plan

Usage: zfa apply [arguments]
-h, --help                   Print this usage information.
    --plan-id (mandatory)    The ID of the plan to execute

Run "zfa help" to see global options.
```

## `zfa benchmark`

```
usage: zfa benchmark SUBCOMMAND [options]

subcommands:
  run [options]                 Run registered benchmark scenarios
  list [--json]                 List registered benchmark scenarios
  register [--discover]         Register scenarios from providers
  baseline save <id> --label L  Save a baseline from a fresh run
  baseline load <id>            Load the latest (or --label) baseline
  baseline compare <id>         Compare a fresh run against a baseline
  baseline list                 List all saved baselines
  report                        Print the latest suite report

run options:
  --scenario <id>               Run only the named scenario (repeatable)
  --tags <a,b>                  Run only scenarios with any of the tags
  --dry-run                     Validate configuration without executing
  --config <json>               Global config JSON merged into every scenario
  --json                        Machine-readable JSON output
  --timeout <ms>                Per-scenario timeout in milliseconds
  --isolate                     Run each scenario in its own isolate (FR-007)
  --concurrency <n>             Worker-pool size for the suite run
  --store <dir>                 Baseline store directory (default:
                                benchmarks/baselines)
```

## `zfa bone`

```
usage: zfa bone SUBCOMMAND [options]

subcommands:
  generate [<slug>]  Generate the bone for the named feature (or active feature)
  export <slug>      Package a generated bone into a tar.gz artifact
  validate <slug>    Re-check a bone for self-containment and staleness

generate options:
  --spec <path>            Override the source spec path (default: specs/<slug>/spec.md)
  --output <dir>           Bone root directory (default: .zfa/bones)
  --di <mock|firebase|auto>  Data backend wiring (default: auto)
  --flutter                Include pubspec.yaml + runnable lib/main.dart + page
  --include-deps           Inline the minimal transitive shared-entity set
  --export                 Also package the bone as <feature>-<di>.tar.gz
```

## `zfa build`

```
Run zuraffa_build to generate code from annotations (calls build_runner)

Usage: zfa build [arguments]
-h, --help               Print this usage information.
-c, --clean              Delete the build cache before building (fixes stale cache errors)
    --flavor             Build a named flavor (spec 030): resolves the features:/flavors: sections of .zfa.json into this build's feature-set — disabled features generate nothing and the generated FeatureFlags registry reflects the flavor.
    --dry-run            Preview what would change without writing files
    --force              Bypass AST merge and regenerate all files from scratch
-a, --[no-]analyze       Run `dart analyze` after build and fail on errors (default: on; use --no-analyze to skip). Catches non-compiling generated code immediately (issue #395).
                         (defaults to on)
    --[no-]dda-routes    Run the DDA @Route stage: scan @Route/@ZfaRoute annotations and compile lib/src/routing/zfa_router.g.dart (spec 033, issue #187). Validation errors fail the build.
                         (defaults to on)
    --dda-routes-only    Run ONLY the DDA @Route stage — skip build_runner and the post-build verifiers entirely.

Run "zfa help" to see global options.
```

## `zfa cache`

```
Generate Cache logic

Usage: zfa cache <subcommand> [arguments]
-h, --help       Print this usage information.
-o, --output     Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                 (defaults to "lib/src")
    --dry-run    Preview generated files without writing to disk
-f, --force      Overwrite existing files
-v, --verbose    Enable detailed logging
    --revert     Revert generated files (delete them)

Available subcommands:
  adapter   Register Hive adapters for an entity and its sub-entities
  create    Create Cache
  verify    Verify Hive adapter coverage for an entity against the entity graph; exit 1 on drift (spec #975).

Run "zfa help" to see global options.
```

## `zfa cli`

```
Generate a standardized CLI command for an entity (FR-011)

Usage: zfa cli [arguments]
-h, --help    Print this usage information.

Run "zfa help" to see global options.
```

## `zfa config`

```
Manage ZFA configuration

Usage: zfa config [arguments]
-h, --help    Print this usage information.

Run "zfa help" to see global options.
```

## `zfa controller`

```
Generate controller class for an entity

Usage: zfa controller <subcommand> [arguments]
-h, --help          Print this usage information.
-o, --output        Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                    (defaults to "lib/src")
    --dry-run       Preview generated files without writing to disk
-f, --force         Overwrite existing files
-v, --verbose       Enable detailed logging
    --revert        Revert generated files (delete them)
-m, --methods       Comma-separated list of methods (get,create,update,delete,list,watch,getList,watchList)
                    (defaults to "get,update")
    --[no-]state    Generate with State integration

Available subcommands:
  create   Create a Controller class

Run "zfa help" to see global options.
```

## `zfa corpus`

```
Import and walk an extracted spec corpus: import, catalog (CORE/SKIN), run (failure budget), ledger (merge gate). See specs/050-corpus-import and specs/076-corpus-walk for the contracts.

Usage: zfa corpus <subcommand> [options]
-h, --help    Print this usage information.

Available subcommands:
  catalog   Catalog the target's specs and classify each CORE (engine seam) or SKIN (presentation seam) — the walk's input contract (epic #1017, child #1015).
  import    Import every feature spec from <source> into specs/ (verbatim), create per-feature tdd/ dirs, and emit the corpus manifest.
  ledger    Record the walk verdicts in the committed ledger and diff against it — regressions on existing contracts are CI failures (the merge gate, epic #1017, child #1017).
  run       Walk every cataloged feature (green/partial/blocked) under the failure budget — the walk finishes; the budget is the gate (epic #1017, child #1016).

Run "zfa help" to see global options.
```

## `zfa create`

```
Create architecture folders or pages

Usage: zfa create [arguments]
-h, --help    Print this usage information.

Run "zfa help" to see global options.
```

## `zfa datasource`

```
Generate DataSources

Usage: zfa datasource <subcommand> [arguments]
-h, --help           Print this usage information.
-o, --output         Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                     (defaults to "lib/src")
    --dry-run        Preview generated files without writing to disk
-f, --force          Overwrite existing files
-v, --verbose        Enable detailed logging
    --revert         Revert generated files (delete them)
-m, --methods        Comma-separated list of methods (get,create,update,delete,list,watch,getList,watchList)
                     (defaults to "get,update")
    --[no-]local     Generate local data source (and Hive/DB integration)
    --[no-]remote    Generate remote data source (and API integration)
                     (defaults to on)
    --[no-]cache     Enable caching
-i, --init           Generate initialization and disposal methods
    --explain        Explain the datasource plan (types, entity field mapping, interface shape) without generating anything

Available subcommands:
  check            Check datasource interface/implementation method parity for an entity
  create           Create a Data Source
  inject           Inject a dependency into the existing datasource
  method           Append a new method to the existing datasource
  private-method   Append a private method to the existing datasource
  verify           Verify the datasource interface matches the entity field signatures (exit 1 with --> fix: on drift; spec #1131)

Run "zfa help" to see global options.
```

## `zfa di`

```
Generate DI registration for a UseCase or Entity via subcommands: zfa di create <Name> | zfa di register <ClassName> | zfa di verify (dangling-binding gate).

Usage: zfa di <subcommand> [arguments]
-h, --help       Print this usage information.
-o, --output     Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                 (defaults to "lib/src")
    --dry-run    Preview generated files without writing to disk
-f, --force      Overwrite existing files
-v, --verbose    Enable detailed logging
    --revert     Revert generated files (delete them)

Available subcommands:
  create     Create DI registrations
  register   Register an existing class in DI
  verify     Verify DI registrations resolve against classes on disk (dangling bindings fail with a fix hint)

Run "zfa help" to see global options.
```

## `zfa doctor`

```
Show information about the installed tooling and v5 migration readiness.

Usage: zfa doctor [arguments]
-h, --help              Print this usage information.
    --fix               Automatically fix detected v5 patterns and heal environment checks where possible (deps, generated-imports, artifacts, baseline cache, profile)
    --format            Output format. json emits a single doctor.v1 verdict object for the named environment checks and sets the exit code (CI-able).
                        [text (default), json]
    --dry-run           Show what would change without writing files (implies --fix)
    --migration-only    Only run v5 migration checks, skip tooling checks

Run "zfa help" to see global options.
```

## `zfa dream`

```
Dream a feature from plain English: draft the spec via the MCP v2 tool, ingest it, run the engine cycle to green, run the skin cycle (opening a hand-edit branch when the view is scaffolded), write the engine/skin receipts, and open a PR (draft until the engine is green). One command over the proven stack (issue #1010).

Usage: zfa dream "<feature description>"
-h, --help               Print this usage information.
    --feature            Feature name (specs/<feature>). Default: the next sequential number + the description slug.
    --project            Project root (default: nearest pubspec/specs walk-up).
    --zfa-bin            The zfa entrypoint the spawned phases run through (default: resolved like zfa tdd run).
    --max-retries        Draft/ingest attempts before an honest stop (default: 3; the ingest refusal re-prompts the drafter).
    --engine-attempts    Engine-cycle run budget before an honest stop (default: 2; zfa tdd run is resumable).
    --no-pr              Skip the git/gh phase entirely (tests, CI, offline runs — receipts and the summary line still record everything).
    --json               Emit a versioned verdict.v1 JSON envelope as the final stdout line (VISION §5, issue #964).

Run "zfa help" to see global options.
```

## `zfa engine`

```
Engine-slice tools (spec 1002 + issue #1109): verify generated engine wiring, certification and static analysis.

Usage: zfa engine check <Entity> [options]
-h, --help    Print this usage information.

Available subcommands:
  check   Static analysis + engine receipt + import boundary + getIt resolution over the generated engine slice; fail with --> fix: on any finding.

Run "zfa help" to see global options.
```

## `zfa entity`

```
zfa entity - Zorphy Entity Generation Commands

USAGE:
  zfa entity <subcommand> [options]

SUBCOMMANDS:
  create      Create a new Zorphy entity with fields
  new         Quick-create a simple entity (basic defaults)
  enum        Create a new Zorphy enum
  add-field   Add field(s) to an existing entity
  from-json   Create entity from JSON file
  list        List all Zorphy entities
  build       Run build_runner build (with optional --clean, --force)
  watch       Run build_runner watch (live rebuild on changes)
  validate    Check entity dirs for missing generated files
  cli         Generate a standardized CLI command for an entity (FR-011)

CREATE COMMAND:
  zfa entity create -n <Name> [options]
  Options:
    -n, --name              Entity name (required)
        --output            Ignored in v5 (entities always generate under lib/src/domain/entities)
    --json                  Enable JSON serialization (default: true)
    --filter                Enable type-safe filters
    --copywith-fn           Function-based copyWith
    --compare               Enable compareTo (default: true)
    --sealed                Create sealed class
    --non-sealed            Create non-sealed class
    --type-key <key>        Custom JSON key for polymorphic dispatch (default: __typename)
    --subtype-wire-value    Custom wire value for this subtype in polymorphic JSON
    --field                 Add field "name:type"
    -F, --fields            Add multiple fields "name:type,name:type"
    --extends               Interface to extend
    --subtypes              Explicit subtypes, e.g. "Admin:1,Guest:2"
                            Each entry is name:wireValue. Used with
                            --type-key for polymorphic dispatch.
    --generate-subs         Generate subtype files
    --auto-id               Auto-generate a String id (uuid v4). The id
                            field is optional at construction and defaults
                            to a fresh uuid (adds uuid to your pubspec).
    --kind=<entity|value_object>
                            Semantic kind. value_object marks an immutable
                            composition type: no id required and `zfa make`
                            generates no repository/usecase/controller/
                            presenter for it.
    --allow-forward-refs    Skip on-disk type validation (batch generation of
                            cyclic schemas — referenced entity is created later).
                            For types that are NEVER entities (external classes
                            like plugin wrappers), use the `!Type` prefix
                            instead (see FIELD SYNTAX below).

ADD-FIELD COMMAND:
  zfa entity add-field -n <Name> [options]
  Options:
    -n, --name              Entity name (required)
    --field                 Add field "name:type"
    -F, --fields            Add multiple fields "name:type,name:type"
    --allow-forward-refs    Skip on-disk type validation (batch generation of
                            cyclic schemas — referenced entity is created later).
                            For types that are NEVER entities (external classes
                            like plugin wrappers), use the `!Type` prefix
                            instead (see FIELD SYNTAX below).

FIELD SYNTAX:
  name:type                 Basic field, Dart name = JSON wire name
                            (e.g. `id:String`, `note:String?`)
  name:!type                External type - the `!` prefix marks a type as
                            external (non-entity, non-enum). The type name is
                            kept as-is (no `$` prefix), on-disk validation is
                            skipped, and no entity/enum import is emitted.
                            Use for types that live outside the entity tree,
                            e.g. plugin wrappers (`WebUri`), SDK classes, etc.
                            Example:
                              url:!WebUri?              # external WebUri type
  name:type:json=<wire>     Dart name differs from the JSON wire name.
                            Required when the wire name is a Dart keyword
                            (`in`, `required`) or starts with `_`
                            (Vendure `_and` / `_or`). Emits
                            `@JsonKey(name: '<wire>')` on the getter so
                            json_serializable serializes with the wire name.
                            Examples:
                              in_:String:json=in            # `in` wire key
                              and:ProductFilterParameter:json=_and
                              required:ConfigArgDef:json=required

  The same syntax is accepted by `--field` (single) and `-F/--fields`
  (comma-separated). `zfa entity from-json` resolves Dart-keyword and
  `_`-prefixed JSON keys automatically (no manual `:json=` needed).

EXAMPLES:
  zfa entity create -n User --field id:String --field name:String
  zfa entity create -n Product --field name:String --field price:double --filter
  zfa entity create -n ChatMessage --auto-id --field role:ChatMessageRole
    --field content:String --field timestamp:DateTime
  zfa entity create -n ParserConfig --kind=value_object
    --field separator:String --field trimWhitespace:bool
  # Vendure-style filter parameter with nested _and/_or composition:
  zfa entity create -n ProductFilterParameter --allow-forward-refs
    --field and:ProductFilterParameter:json=_and
    --field or:ProductFilterParameter:json=_or
  # External (non-entity) type - no $ prefix, no import, no validation:
  zfa entity create -n JsAlertRequest --kind=value_object
    --field url:!WebUri? --field message:String?
  # IdOperators with `in` (Dart keyword) remapped to `in_`:
  zfa entity create -n IdOperators --field in_:String:json=in --field eq:String
  zfa entity enum -n Status --value pending,active,completed
  zfa entity add-field -n User --field email:String?
  zfa entity list

NOTES:
  - Entities always live under lib/src/domain/entities in Zuraffa v5.
  - Legacy --output values are accepted for compatibility but ignored.
  - Raw Dart-keyword field names (e.g. `--field in:String`) are rejected;
    remap with `:json=<wire>` so the generated source compiles.

For more information, visit: https://github.com/arrrrny/zorphy
```

## `zfa feature`

```
Wrapper over zfa make using the normalized feature preset.

Usage: zfa feature [scaffold|route|di|mock|test|view|presenter|controller|state] <Name> [options] | zfa feature list [--format=json] | zfa feature enable|disable <name>
-h, --help                Print this usage information.
    --name                Feature name (alternative to the positional argument)
    --feature             Feature contract id for feature-scoped capabilities (e.g. 004-login-ui) — validated against the registered contracts
    --project-root        Project root directory
-o, --output              Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                          (defaults to "lib/src")
    --dry-run             Preview generated files without writing to disk
-f, --force               Overwrite existing files
-v, --verbose             Enable detailed logging
    --revert              Revert generated files (delete them)
    --format              Output format: text, json
                          (defaults to "text")
    --plan                Print the normalized execution plan and exit
    --explain             Explain the normalized execution plan and exit
    --[no-]vpcs           Generate View, Presenter, Controller, State
                          (defaults to on)
    --[no-]repository     Generate Repository
                          (defaults to on)
    --[no-]datasource     Generate DataSource (Remote and/or Local)
                          (defaults to on)
    --local               Generate local data source (instead of remote)
    --mock                Generate Mock data
    --use-mock            Alias of --mock (the manifest inputSchema property name)
    --[no-]di             Generate Dependency Injection setup
                          (defaults to on)
    --cache               Enable Caching (generates local + remote datasources)
    --use-service         Use service and provider instead of repository and datasource
    --route               Generate Routing definitions
    --test                Generate Tests
-u, --usecases            List of custom usecases to generate
-m, --methods             List of entity methods to generate (e.g. get,create,update,delete,list,watch,getList,watchList)
                          (defaults to "get", "update")
    --query-field         Query field name for get/watch methods
    --query-field-type    Query field type for get/watch methods
    --id-field            ID field name
    --id-field-type       ID field type

Run "zfa help" to see global options.
```

## `zfa generate-commands`

```
Regenerate speckit extension command files from zfa manifest

Usage: zfa generate-commands [arguments]
-h, --help       Print this usage information.
-o, --output     Output directory for generated .md files
                 (defaults to ".specify/extensions/zuraffa/commands/")
    --dry-run    Preview without writing files
-v, --verbose    Enable verbose logging

Run "zfa help" to see global options.
```

## `zfa graphql`

```
Generate GraphQL files

Usage: zfa graphql <subcommand> [arguments]
-h, --help          Print this usage information.
-o, --output        Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                    (defaults to "lib/src")
    --dry-run       Preview generated files without writing to disk
-f, --force         Overwrite existing files
-v, --verbose       Enable detailed logging
    --revert        Revert generated files (delete them)
-t, --type          GraphQL operation type (query, mutation)
                    (defaults to "query")
    --returns       Return type
    --input-type    Input type name
    --input-name    Input variable name
    --op-name       Operation name

Available subcommands:
  create       Create GraphQL
  diff         Diff two cached schema versions and report breaking changes (exit 1 when breaking changes exist)
  introspect   Introspect a remote GraphQL endpoint and print a generation plan
  pull         Fetch a GraphQL schema via introspection and cache it (SDL + introspection JSON)

Run "zfa help" to see global options.
```

## `zfa gym`

```
Generate Gym artifacts

Usage: zfa gym <subcommand> [arguments]
-h, --help       Print this usage information.
-o, --output     Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                 (defaults to "lib/src")
    --dry-run    Preview generated files without writing to disk
-f, --force      Overwrite existing files
-v, --verbose    Enable detailed logging
    --revert     Revert generated files (delete them)
-d, --domain     Domain folder for the gym target
                 (defaults to "general")

Available subcommands:
  create   Create a Gym

Run "zfa help" to see global options.
```

## `zfa initialize`

```
Initialize a project for Zuraffa: wire dependencies + scaffold a test entity

USAGE:
  zfa initialize [options]
  zfa init [options]

OPTIONS:
-e, --entity       Entity name to generate (default: Product)
                   (defaults to "Product")
-o, --output       Entity output directory (fixed to lib/src/domain/entities in v5; custom values are ignored)
                   (defaults to "lib/src/domain/entities")
-f, --force        Overwrite existing files
    --dry-run      Preview what would be generated without writing files
    --deps-only    Only wire zuraffa dependencies into pubspec.yaml; skip entity scaffolding.
    --no-deps      Skip dependency wiring; only scaffold the test entity (legacy behavior).
    --dart         Bootstrap a pure-Dart package in-place: if pubspec.yaml is missing, synthesize a minimal one, then wire the pure-Dart dependency set. Cannot be combined with --flutter.
    --flutter      Force the Flutter dependency set (default is auto-detect from pubspec.yaml). Cannot be combined with --dart.
-v, --verbose      Enable verbose output
-h, --help         Show help
    --root         Project root to initialize in (default: current directory). Lets tests run against an explicit sandbox instead of relying on the process working directory.

EXAMPLES:
  zfa initialize                           # Wire deps + generate Product entity
  zfa init                                 # Same as above (alias)
  zfa initialize --entity=User             # Wire deps + generate User entity
  zfa init --deps-only                     # Wire deps only, skip entity
  zfa init --no-deps -e Order              # Skip deps, only scaffold entity
  zfa initialize --dart                    # Bootstrap pure-Dart package in-place
  zfa init --dart --deps-only              # In-place bootstrap, no test entity
  zfa initialize --dry-run                 # Preview without writing files

DESCRIPTION:
  Wires the standard zuraffa dependency set (build_runner, zuraffa/zuraffa_flutter,
  zorphy_annotation, analyzer override) into pubspec.yaml, creates build.yaml with
  zorphy builder registration, ensures .zfa.json exists, then creates a sample
  entity with common fields under lib/src/domain/entities.

  For a brand-new app, prefer `zfa setup <name>` which runs flutter/dart create
  AND wires dependencies in one step. To initialize an EXISTING pure-Dart
  repository that has no pubspec.yaml yet, use `zfa init --dart` (synthesizes a
  minimal pubspec.yaml in-place from the directory name).

  Use --deps-only to wire dependencies without scaffolding an entity.
  Use --no-deps to scaffold only the entity (legacy behavior).
```

## `zfa make`

```
Run multiple generator plugins explicitly.

Usage: zfa make <Name> <plugin1> <plugin2> ... [options]
-h, --help                     Print this usage information.
    --ui                       Scaffold a composite UI component (spec 024): node entity + renderer extension + schema registration
-o, --output                   Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                               (defaults to "lib/src")
    --dry-run                  Preview generated files without writing to disk
-f, --force                    Overwrite existing files
-v, --verbose                  Enable detailed logging
    --revert                   Revert generated files (delete them)
    --format                   Output format: text, json
                               (defaults to "text")
-j, --from-json                JSON configuration file
    --from-stdin               Read JSON configuration from stdin
    --preset                   Generation preset to expand
    --with                     Additional plugins or aliases
    --without                  Plugins or aliases to exclude
    --plan                     Print the normalized execution plan and exit
    --explain                  Explain the normalized execution plan and exit
    --json                     Emit the resolved plan — including the repository emission plan (spec 0973) — as a single JSON object; implies --format json for plan/explain output
    --methods                  Entity methods to generate
    --engine-feature           Feature directory the engine receipt is written to (specs/<feature>/tdd/engine.receipt.json; issue #1109). Defaults to the active feature contract, the pinned .specify/feature.json, or the entity snake name.
    --usecases                 UseCases to inject into presenter/controller
    --variants                 Polymorphic variants to generate
    --domain                   Domain subfolder
    --repo                     Repository to inject
    --service                  Service to inject
    --id-field                 ID field name
                               (defaults to "id")
    --id-field-type            ID field type
                               (defaults to "String")
    --query-field              Query field name
                               (defaults to "id")
    --query-field-type         Query field type
    --no-entity                Skip entity
    --vpc                      Generate full VPC set
    --vpcs                     Generate full VPC set
    --state                    Generate state class
    --data                     Generate data layer
    --datasource               Generate data source
    --cache                    Enable caching
    --sqlite                   Generate a SQLite-backed local data source (package:sqlite3)
    --sync                     Enable offline-first sync
    --bidirectional            Enable bidirectional sync (push + pull)
    --sync-batch-size          Sync batch size (default: 50)
                               (defaults to "50")
    --sync-max-retries         Max sync retry attempts (default: 5)
                               (defaults to "5")
    --route                    Generate route
    --mock                     Generate mock data
    --test                     Generate tests
    --append                   Append to existing repo/service
    --xray                     Generate views with X-Ray scope/node decoration (issue #360)
    --skin                     Generate views with the runtime skin-contract auditor wrap (issues #1102/#1166); the auditor kit file is emitted when missing
    --anchor                   A zfa: anchor the generated --skin view declares (repeatable; issue #1112) — emits the anchorExists contract row and the debugTap<PascalAnchor>() VM-service driver function per anchor.
    --compile-only             Opt out of the mocked tier (issue #1194): generate the slice without the certified mock datasource / simulation-mode binding — compile-only, not demo-green. The mocked tier is the default (boots on certified mocks via --dart-define=SIMULATION=true).
    --[no-]gql                 Deprecated alias for --graphql (issue #1149): the gql plugin was folded into graphql. Use --graphql instead.
                               (defaults to on)
    --[no-]repository          Enable or disable Repository Plugin
                               (defaults to on)
    --[no-]use-service         Use service instead of repository
    --[no-]provider            Enable or disable Provider Plugin
                               (defaults to on)
    --params                   Parameter type for the provider method
    --returns                  Return type for the provider method
    --type                     Provider method type (sync, stream, completable, usecase)
    --[no-]init                Generate initialization and disposal methods
    --[no-]usecase             Enable or disable UseCase Plugin
                               (defaults to on)
    --[no-]presenter           Enable or disable Presenter Plugin
                               (defaults to on)
    --[no-]controller          Enable or disable Controller Plugin
                               (defaults to on)
    --[no-]view                Enable or disable View Plugin
                               (defaults to on)
    --[no-]adaptive-layouts    Scaffold platform/device-specific layout files
    --[no-]platform-shells     Enable shell-oriented adaptive layout scaffolding
    --layout-targets           Adaptive layout targets, e.g. mobile,tablet,desktop,macos
    --[no-]v6-state            Generate v6 dual-layer state (DomainState + ViewState + DualLayerPresenter) and ControlledWidget/FragmentBuilder-based views instead of the legacy v5 monolithic state
    --[no-]feature             Enable or disable Feature Plugin
                               (defaults to on)
    --[no-]gym                 Enable or disable Gym Plugin
                               (defaults to on)
    --[no-]mock-data-only      Only generate mock data, not providers
    --[no-]mock-json           Generate JSON mock data with fromJson-based helpers
    --[no-]fail                Emit the <Entity>FailingMockProvider: a throwing double whose every method throws the sealed failure type (the failure-path twin of the certified mock; spec 1110)
    --[no-]api                 Enable or disable API Plugin
                               (defaults to on)
    --[no-]di                  Enable or disable DI Plugin
                               (defaults to on)
    --[no-]use-mock            Use mock providers in DI
    --framework                (defaults to "get_it")
    --[no-]local               Generate local data source
    --[no-]remote              Generate remote data source
                               (defaults to on)
    --shell                    Shell binding for the route: which shell kind targets it (none, bottom-nav, rail, adaptive).
                               (defaults to "none")
    --[no-]deep-link           Whether the route participates in deep-link registration (--scheme/--host writes the Android intent-filter + iOS CFBundleURLSchemes entry).
    --platform-matrix          Platform slots the route targets (spec #1000 K-TRACK): the Skin Contract platform row columns.
                               (defaults to "mobile", "ios", "android", "macos")
    --guard                    Route guard binding: the redirect guard name guarding the route, or none.
                               (defaults to "none")
    --state-machine            State binding the route view references (spec #1004 Skin Contract stateRows kinds: observer, listener), or none.
                               (defaults to "none")
    --cache-policy             (defaults to "daily")
    --cache-storage            (defaults to "hive")
    --ttl                      (defaults to "1440")
    --sync-direction           (defaults to "push")
    --[no-]graphql             Enable or disable GraphQL Generator
                               (defaults to on)
    --schema-path              Path to .graphql schema file
    --gql-type                 GraphQL operation type (query, mutation, subscription)
    --gql-returns              GraphQL return fields
    --input-type               Input type name
    --input-name               Input variable name
    --op-name                  Operation name
    --layout                   UI layout type
                               (defaults to "list")
    --[no-]filter              Enable filtering
    --[no-]sort                Enable sorting
    --ignore-fields            Fields to exclude from UI
    --[no-]strategy            Enable or disable Strategy Plugin
                               (defaults to on)
    --strategies               Variant names to generate (e.g. [scraper, ai])
    --[no-]method_append       Enable or disable Method Append Plugin
                               (defaults to on)
    --[no-]module              Enable or disable Module Plugin
                               (defaults to on)
    --[no-]mcp                 Enable or disable MCP Plugin
                               (defaults to on)
    --[no-]cli                 Enable or disable Standard CLI Plugin
                               (defaults to on)
    --[no-]tui                 Enable or disable TUI
                               (defaults to on)
    --[no-]benchmark           Enable or disable Benchmark
                               (defaults to on)
    --[no-]agent               Enable or disable Agent Plugin
                               (defaults to on)
    --[no-]skeleton            Enable or disable Skeleton
                               (defaults to on)
    --[no-]slice               Enable or disable Slice
                               (defaults to on)
    --[no-]sliceByDefault      Enable the slice plugin in the default toolchain
    --default-depth            Extraction depth used when --depth is omitted
                               (defaults to "feature")

Run "zfa help" to see global options.
```

## `zfa manifest`

```
List all available capabilities

Usage: zfa manifest [arguments]
-h, --help      Print this usage information.
-f, --format    Output format
                [json (default), mcp]
    --verify    Certify the treaty: manifest inputSchemas ↔ CLI flags ↔ help text (drift exits 3). Optional trailing plugin ids scope the certification.

Run "zfa help" to see global options.
```

## `zfa mcp`

```
Scaffold / serve / inspect an MCP server that exposes this app's features as AI-callable tools (issue #369)

Usage: zfa mcp <subcommand> [arguments]
-h, --help       Print this usage information.
-o, --output     Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                 (defaults to "lib/src")
    --dry-run    Preview generated files without writing to disk
-f, --force      Overwrite existing files
-v, --verbose    Enable detailed logging
    --revert     Revert generated files (delete them)

Available subcommands:
  list-tools   List the MCP tools the scaffolded server would expose (JSON)
  replay       Re-execute a committed MCP tool-call scenario (JSON) against the scaffolded server and write a verdict receipt (issue #1358)
  scaffold     Scaffold a runtime MCP server (lib/src/mcp/tools.dart + bin/mcp_server.dart) so the app can expose its features as MCP tools callable by AI agents.
  serve        Run the scaffolded MCP server (stdio by default, or --sse for HTTP+SSE)

Run "zfa help" to see global options.
```

## `zfa migrate`

```
Migrate v5 artifacts to v6 equivalents (state, gql, di).

Usage: zfa migrate [arguments]
-h, --help       Print this usage information.
    --dry-run    Preview changes without writing files
-v, --verbose    Show detailed output

Run "zfa help" to see global options.
```

## `zfa mock`

```
Generate Mocks

Usage: zfa mock <subcommand> [arguments]
-h, --help       Print this usage information.
-o, --output     Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                 (defaults to "lib/src")
    --dry-run    Preview generated files without writing to disk
-f, --force      Overwrite existing files
-v, --verbose    Enable detailed logging
    --revert     Revert generated files (delete them)

Available subcommands:
  certify      Re-certify a mock live (contract test in a sandbox) and add it to the #832 registry entry (spec 1001)
  create       Create a Mock
  data         Generate only mock data (fixtures) for an entity
  dependency   Generate the certified mock for a declared dependency row (issue #960)
  explain      Explain a mock: coverage, skipped members, per-method certification status, fixture-selector bindings (spec 1121)
  inject       Inject a dependency into the existing mock
  json         Generate JSON mock data with fromJson-based Dart helpers
  method       Append a new method to the existing mock
  verify       Re-run the certification gate against the mock files on disk (read-only) — exit 1 with --> fix: lines on drift (spec 1121)

Run "zfa help" to see global options.
```

## `zfa module`

```
Scaffold a new Zuraffa feature package with plugin orchestrator

Usage: zfa module <FeatureName> [options]
-h, --help       Print this usage information.
-o, --output     Parent directory for the new package (default: current directory)
                 (defaults to ".")
    --dry-run    Preview the scaffold without writing files
-f, --force      Overwrite existing files
-v, --verbose    Enable detailed logging
    --no-gate    Skip the post-scaffold `dart pub get` + `dart analyze` gate

Run "zfa help" to see global options.
```

## `zfa observer`

```
REMOVED (issue #1149) — subscribe directly to the UseCase result stream instead

Usage: zfa observer <subcommand> [arguments]
-h, --help    Print this usage information.

Run "zfa help" to see global options.
```

## `zfa package`

```
Zuraffa-native package SDK (spec 025): create reusable packages that contribute architecture to consuming apps via auto-DI, runtime modules, and namespaced agent tools

Usage: zfa package <subcommand> [arguments]
-h, --help    Print this usage information.

Available subcommands:
  create   Create a new Zuraffa-native package scaffold (standard domain/data layout, runtime module, package registrar, test harness) that passes analysis and codegen with zero manual edits

Run "zfa help" to see global options.
```

## `zfa plugin`

```
zfa plugin - Manage ZFA plugins

USAGE:
  zfa plugin <command> [options]

COMMANDS:
  list               List available plugins
  enable <id>        Enable a plugin
  disable <id>       Disable a plugin
  add <package>      Wire a plugin package into main.dart
  mcp [--force]      Scaffold a runtime MCP server into the host app
                     (alias for `zfa mcp scaffold`; issue #369)
```

## `zfa presenter`

```
Generate presenter class for an entity

Usage: zfa presenter <subcommand> [arguments]
-h, --help       Print this usage information.
-o, --output     Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                 (defaults to "lib/src")
    --dry-run    Preview generated files without writing to disk
-f, --force      Overwrite existing files
-v, --verbose    Enable detailed logging
    --revert     Revert generated files (delete them)
-m, --methods    Comma-separated list of methods (get,create,update,delete,list,watch,getList,watchList)
                 (defaults to "get,update")
    --[no-]di    Generate with DI integration
                 (defaults to on)

Available subcommands:
  create   Create a Presenter class

Run "zfa help" to see global options.
```

## `zfa proof`

```
Generation receipts: prove where generated artifacts came from (issue #807), and validate the whole proof chain end-to-end (issue #1148).

Usage: zfa proof <subcommand> [arguments]
-h, --help    Print this usage information.

Available subcommands:
  chain   Validate the whole proof chain end-to-end: receipt digests, spec behavior green evidence, generated-test integrity, route and usecase verifies, and xray coverage — exit 0 intact / 1 drift / 2 infra error (issue #1148).
  check   Verify generation receipts: digest drift, deletions, stale specs and — with coverage roots — unprovenanced generated-code paths.
  prune   Delete receipts whose every artifact is missing (dead sandbox or interrupted-run receipts). Dry run by default; --apply deletes (issue #1378).

Run "zfa help" to see global options.
```

## `zfa provider`

```
Generate Providers

Usage: zfa provider <subcommand> [arguments]
-h, --help       Print this usage information.
-o, --output     Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                 (defaults to "lib/src")
    --dry-run    Preview generated files without writing to disk
-f, --force      Overwrite existing files
-v, --verbose    Enable detailed logging
    --revert     Revert generated files (delete them)

Available subcommands:
  create           Create a Provider
  inject           Inject a dependency into the existing provider
  method           Append a new method to the existing provider
  private-method   Append a private method to the existing provider
  verify           Verify a provider: no surviving UnimplementedError stub bodies (stub-escape gate) and every Service interface method implemented (conformance gate) — exit 1 with --> fix: lines otherwise (spec 979).

Run "zfa help" to see global options.
```

## `zfa replay`

```
Deterministically re-execute a feature's recorded TDD history: zfa replay <feature> or zfa replay <path>/tdd/cycle-log.md. History becomes executable documentation that either runs or screams.

Usage: zfa replay [arguments]
-h, --help            Print this usage information.
    --behavior        Replay only this behavior id (default: every recorded behavior)
    --project         The project root (default: nearest pubspec.yaml walk-up)
    --zfa-bin         The zfa entrypoint recorded gen steps resolve to (default: PATH)
    --timeout         Per-recorded-command timeout in minutes (bug #742)
    --events          Write the NDJSON event log to this path (spec 066 FR-014)
    --keep-sandbox    Keep the replay sandbox for debugging (path in the summary)

Run "zfa help" to see global options.
```

## `zfa repository`

```
Generate Repositories

Usage: zfa repository <subcommand> [arguments]
-h, --help       Print this usage information.
-o, --output     Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                 (defaults to "lib/src")
    --dry-run    Preview generated files without writing to disk
-f, --force      Overwrite existing files
-v, --verbose    Enable detailed logging
    --revert     Revert generated files (delete them)

Available subcommands:
  create   Create a Repository Interface and Implementation (the direct `zfa repository create Product` entry point)
  method   Append a new method to the existing repository

Run "zfa help" to see global options.
```

## `zfa route`

```
Generate route definitions for an entity

Usage: zfa route <subcommand> [arguments]
-h, --help       Print this usage information.
-o, --output     Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                 (defaults to "lib/src")
    --dry-run    Preview generated files without writing to disk
-f, --force      Overwrite existing files
-v, --verbose    Enable detailed logging
    --revert     Revert generated files (delete them)

Available subcommands:
  create      Create route definitions for an entity. Emits a machine verdict envelope with --json (spec 0971).
  custom      Create a custom route (non-entity based)
  deep-link   Generate a deep-link GoRoute module and register the URL scheme in AndroidManifest.xml + Info.plist (idempotent)
  shell       Generate a StatefulShellRoute.indexedStack shell with one branch per --branch <Label>:<path>, plus an optional bottom navigation bar (Material 3 NavigationBar) or desktop NavigationRail (--adaptive). Registers the shell in getAllRoutes().
  verify      Verify routes. With <Entity>: prove the declared route-table receipt against the current tree and run the route-table test headlessly (spec 0971). Without: compare CLI-generated and DDA-annotated routes for drift. Drift verdicts and exit codes: match → exit 0 (both systems present, path sets agree); drift → exit 1 (overlapping or one-sided paths, each named); insufficient-input → exit 2 (a system has no routes at all; with --strict it exits 1). Insufficient-input is never a silent PASS.

Run "zfa help" to see global options.
```

## `zfa schema`

```
Output JSON schema for configuration

Usage: zfa schema [arguments]
-h, --help    Print this usage information.

Run "zfa help" to see global options.
```

## `zfa service`

```
Generate Services

Usage: zfa service <subcommand> [arguments]
-h, --help       Print this usage information.
-o, --output     Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                 (defaults to "lib/src")
    --dry-run    Preview generated files without writing to disk
-f, --force      Overwrite existing files
-v, --verbose    Enable detailed logging
    --revert     Revert generated files (delete them)

Available subcommands:
  create   Create a Service interface — canonical --json verdict (issue #1105), --explain, per-entity receipts and a grammar-conformance proof
  method   Append a new method to the existing service
  verify   Verify a service file conforms to the schema grammar (class, methods, signatures) — exit 1 with --> fix: lines otherwise (spec 1127)

Run "zfa help" to see global options.
```

## `zfa setup`

```
Bootstrap a new Flutter/Dart app with zuraffa dependencies wired in

Usage: zfa setup <name> [options]
-h, --help                            Print this usage information.
    --flutter                         Create a Flutter app (default). Pass `--platforms`/`--org`, or any other `flutter create` flag via a `--` separator — everything after `--` is forwarded verbatim to `flutter create`.
    --dart                            Create a pure Dart package (dart create -t package).
    --platforms=<ios,android>         Comma-separated platforms for `flutter create` (forwarded as-is; ignored with --dart). For any other `flutter create` flag, use the `--` passthrough.
    --org=<com.example>               Organization name for `flutter create` (e.g. com.example; ignored with --dart).
    --dry-run                         Preview what would be created/wired without writing files.
-f, --force                           Delete and recreate the target directory if it already exists (requires confirmation on a terminal).
-v, --verbose                         Enable verbose output.
    --no-git                          Skip git initialization of the created project (useful for CI/automation that manages its own VCS).
    --deep-link-scheme=<gozuzu>       Pre-seed a URL scheme in AndroidManifest.xml + Info.plist. Flutter-only (ignored with --dart).
    --deep-link-host=<go.zuzu.dev>    Optional host for App Links (paired with --deep-link-scheme + --auto-verify).
    --auto-verify                     Set android:autoVerify="true" on the intent-filter (App Links). Paired with --deep-link-scheme.
    --tdd-example                     Emit an additional failing example test (test/tdd_example_test.dart) whose failure is an assertion failure (not a compile error), to demonstrate the red half of the red-green-refactor loop. See specs/041-tdd-setup-plugin/spec.md.
    --specs=<dir>                     Import an extracted spec corpus (a directory of feature directories, each containing spec.md) into the new app: copies every spec verbatim, creates per-feature tdd/ dirs, and emits the corpus manifest for batch driving. See specs/050-corpus-import/spec.md.

Run "zfa help" to see global options.
```

## `zfa simulate`

```
Spin a certified simulation world (VISION §9): scaffold fixture worlds per service family (FirebaseAuth, Vendure, Rest, AdMob, Otel), replay the golden contract offline, and certify network isolation. Spec 968: `simulate init/run <scenario>` — committed scenario worlds with virtual time, latency, failure storms.

Usage: zfa simulate [options] | zfa simulate <init|run|replay|certify|verify-world> <scenario> [options]
-h, --help                      Print this usage information.
    --scaffold=<feature-dir>    Materialize the certified fixture worlds into <feature-dir>/tdd/fixtures/ and hash them into the cycle-log evidence.
    --family                    Simulation family for --scaffold (repeatable; default: all).
                                [firebase-auth, vendure, rest, admob, otel]
    --feature=<feature-dir>     Load the committed world from <feature-dir>/tdd/fixtures/. A bare feature name resolves under specs/ (the #968 fixtures the scaffold wrote).
    --fixtures=<dir>            Load the committed world from an explicit fixtures directory (overrides --feature).
    --scenario                  Golden replay to run: "golden" (every fixture) or one family name.
                                (defaults to "golden")
    --verify-guard              Self-certify the network-isolation guard and exit.
-f, --force                     Re-scaffold over an existing certified world.

Run "zfa help" to see global options.
```

## `zfa skin`

```
Generate Skin UI widgets (zuraffa_ui) for entities; also hosts the runtime skin-contract auditor (skin kit | verify | drive, issue #1102/#1112)

Usage: zfa skin <layout> <Entity> [options] | zfa skin ui.schema.export [--project-root <dir>] [--schema-version <v>] | zfa skin kit|verify|drive [options]
-h, --help    Print this usage information.

Available subcommands:
  drive              Drive an anchor in the LIVE app through the VM service (debugTapAnchorJson evaluate); prints the TapResult JSON as the final stdout line — replaces synthetic clicks (issue #1112).
  form               Generate a Skin form widget for an entity
  grid               Not implemented (issue #1149) — invoking it refuses loudly; implemented layouts: list, form
  kit                Emit the runtime skin-contract auditor Flutter glue (skin/skin_contract_auditor.dart)
  list               Generate a Skin list widget for an entity
  table              Not implemented (issue #1149) — invoking it refuses loudly; implemented layouts: list, form
  ui.schema.export   Export the skin plugin UI component vocabulary as a versioned, diff-stable JSON Schema
  verify             Statically reconcile the kit route-contract table against the routing barrel (match / drift / insufficient-input)

Run "zfa help" to see global options.
```

## `zfa slice`

```
usage: zfa slice SUBCOMMAND [options]

subcommands:
  cut <name>      Extract a runnable slice (see cut options below)
  compose <id>    Resolve a feature contract → slice (engine/skin/contract/receipts)
  worktree <id>   Open a git worktree rooted at the slice (agent = the feature)
  id <id>         Print the feature's stable slice id (the FeatureContract id)
  check <id>      Validate the slice against its contract (compliance report)
  merge <name>    Merge agent changes from a slice back into the project
  list            List active slices
  inspect <name>  Show a slice's files, ownership, and modification status
  verify <name>   Full audit of a feature slice: aggregates the five
                  sub-receipts (engine + skin + cert + xray + journal) into
                  slice.receipt.json; exits 0 only when all are green — the
                  merge gate (spec 1116). A cut slice keeps the import check
                  (--analyze for dart analyze).
  run <name>      Launch the slice (flutter run -t <main_slice.dart>)
  export <name>   Export a slice (--format tar.gz|github [--repo <name>])
  import <name>   Pull an exported GitHub repo back (--from github)

cut options:
  --entry <page|path>  Entry point (repeatable; page name or file path)
  --depth <level>      view | presentation | feature (default) | full
  --verify             Verify the slice after cutting; fail if incomplete
  --feature <id>       Feature id whose runnable sandbox is composed (073)
  --route <p:Page>     Declared route (repeatable; needs --feature)
  --dependency <row>   Declared dependency row Name:kind:contract:priority:artifact
                       (repeatable; needs --feature)

merge options:
  --yes                Confirm shared-file overwrites without prompting

verify options:
  --analyze            Also run dart analyze on the sandbox

export options:
  --format <fmt>       tar.gz or github (required)
  --repo <name>        Target repo (github format; auto-named when omitted)

import options:
  --from <source>      github (currently the only source)
```

## `zfa spec`

```
Spec-quality arena: fuzz the spec contract with deterministic mutations and referee every round (killed = the tests pin the intent; survived = a proven spec weakness). See specs/0967-spec-mutation-arena for the contract.

Usage: zfa spec <subcommand> [options]
-h, --help    Print this usage information.

Available subcommands:
  fuzz   Mutation testing for intent: apply deterministic spec mutations, re-run the loop, report killed/survived (spec 0967, VISION §7). Survived mutants are proven spec weaknesses.

Run "zfa help" to see global options.
```

## `zfa sqlite`

```
Generate a SQLite-backed DataSource for an entity (adapter)

Usage: zfa sqlite <subcommand> [arguments]
-h, --help       Print this usage information.
-o, --output     Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                 (defaults to "lib/src")
    --dry-run    Preview generated files without writing to disk
-f, --force      Overwrite existing files
-v, --verbose    Enable detailed logging
    --revert     Revert generated files (delete them)

Available subcommands:
  create   Create a SQLite data source adapter

Run "zfa help" to see global options.
```

## `zfa state`

```
Generate State classes

Usage: zfa state <subcommand> [arguments]
-h, --help       Print this usage information.
-o, --output     Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                 (defaults to "lib/src")
    --dry-run    Preview generated files without writing to disk
-f, --force      Overwrite existing files
-v, --verbose    Enable detailed logging
    --revert     Revert generated files (delete them)

Available subcommands:
  create   Create a State class (entity / orchestrator / custom emission)
  verify   Verify a state class against its create receipt (match / stale / missing per contract method) — exit 1 with --> fix: lines on drift (spec 1126)

Run "zfa help" to see global options.
```

## `zfa strategy`

```
Generate FetchStrategy abstract base, concrete variants, and StrategySelector

Usage: zfa strategy <subcommand> [arguments]
-h, --help                      Print this usage information.
-o, --output                    Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                                (defaults to "lib/src")
    --dry-run                   Preview generated files without writing to disk
-f, --force                     Overwrite existing files
-v, --verbose                   Enable detailed logging
    --revert                    Revert generated files (delete them)
    --strategies (mandatory)    Comma-separated strategy variant names (e.g. scraper,ai)
    --params                    Input type for FetchStrategy<Input, Output> (e.g. UrlSpark)
    --returns                   Output type for FetchStrategy<Input, Output> (e.g. Listing)
    --domain                    Provider domain folder (defaults to name in snake_case)

Available subcommands:
  create   Generate FetchStrategy abstract base, concrete variants, and StrategySelector

Run "zfa help" to see global options.
```

## `zfa sync`

```
Generate offline-first sync logic

Usage: zfa sync <subcommand> [arguments]
-h, --help           Print this usage information.
-o, --output         Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                     (defaults to "lib/src")
    --dry-run        Preview generated files without writing to disk
-f, --force          Overwrite existing files
-v, --verbose        Enable detailed logging
    --revert         Revert generated files (delete them)
    --direction      Sync direction (push or bidirectional)
                     [push (default), bidirectional]
    --batch-size     Number of records per sync batch
                     (defaults to "50")
    --max-retries    Maximum sync retry attempts before failing
                     (defaults to "5")

Available subcommands:
  enable     Enable offline-first sync for an entity
  simulate   Chaos-drive the sync strategy against a scripted failing remote (scenario: offline-flap) and report the per-key landing ledger

Run "zfa help" to see global options.
```

## `zfa tdd`

```
Drive the full TDD red-green-refactor cycle (init, plan, gen, verify-red, make, wire, func, refactor, run, run-engine, run-skin, split, status, prove, verify). See specs/041-tdd-setup-plugin/spec.md for the full contract; specs/1000-spec-template-core-skin-lanes/spec.md for the lane split, specs/1008-two-cycle-driver/spec.md for the two-cycle runner, and specs/1113-unified-tdd-journal/spec.md for the unified journal.

Usage: zfa tdd <subcommand> [options]
-h, --help    Print this usage information.

Available subcommands:
  compose         Compose an acceptance behavior's subject against the feature's green unit subjects — the composition step of the acceptance make pipeline (issue #642, spec 052).
  corpus          Drive the whole spec corpus through the TDD loop: batch run with resume, per-feature verify gate, provenance audit, and the gap ledger (spec 051).
  diff-check      Check fixture parity between the mock and real adapters for the feature's committed adapter contracts; drift = named verdict, exit 2 (bug #915).
  doctor          Diagnose a feature's TDD stores and prescribe exactly one recovery action — migrate (another feature owns the legacy-layout files), adopt (register unowned generated files), reset (drop stale registry records), or resume (re-run the loop) — as a --> fix: line with a JSON verdict (bugs #840, #874).
  fake            Generate a framework-certified fake for a platform channel: a test-side handler (TestDefaultBinaryMessengerBinding) that replays a committed scenario script — responses, errors, permission states — and records the observed calls (issue #831).
  func            Scaffold the plain-function subject of a behavior (render, format, parse, compute, ...) with a description-derived return type — the function-generation surface of the pipeline (bug #657).
  gen             Generate a failing test + compiling source stub for a behavior (spec 044-test-tdd-generation, FR-001..011).
  ingest          Validate a draft spec through the plan gates (+ entity-collision and contract-ambiguity gates) and place it as specs/<feature>/spec.md — the dream loop's ingestion step.
  init            Idempotently ensure the TDD baseline exists in the current project (test/, dart_test.yaml, .specify/memory/tdd-profile.md, testing dev_dependencies).
  make            Generate minimal implementation via zfa make/entity create/build, run the target test green, certify the suite stays clean, and append green evidence to tdd/cycle-log.md (spec 047).
  migrate-paths   Move recorded TDD artifacts from the legacy flat layout (test/tdd/<id>_test.dart) to the per-feature namespaced layout (test/tdd/<feature-slug>/<id>_test.dart) and rewrite the registry records (bug #827).
  plan            Read <feature>/spec.md and emit <feature>/tdd/test-list.md (one behavior per criterion). <feature> is a plain specs/ feature name, a specs/<feature> path, a .specify/bugs/<slug> bug directory, or an absolute path (issue #1182).
  prove           Walk the feature's unified journal and compute the incremental re-proving delta — the behaviors ungated since the last prove (no green evidence, or files changed since the last prove's fingerprints). Only those behaviors need re-proving; prove itself runs nothing. Exit 0 iff zero ungated (spec 1113, issue #1113).
  realize         Run the mock-to-real swap flow (spec 913; --adapter required), gated by the contract suite and real-vs-mock differential; --diff-only replays ONLY the standalone differential harness (spec 1195; --adapter optional) without touching the tree. Spec 1193 adds the certified-mock location, --scaffold (the hand-delta seam), --dry-run (preview, zero writes), the unified-journal ladder advance MOCKED->REAL->DONE, and the realize hand-delta receipt with generated/mock/hand ratios.
  realize-mock    Run the Tier-1 contract test, then the same contract cases through a Firestore-shaped Tier2MockProvider (backed by a fake FirebaseFirestore), and certify the pair with a per-method differential receipt (issue #1009). Divergence = exit 1 with the mismatched method named.
  refactor        Refactor on a green suite only; never edit tests. Applies the fixed pass registry (resolved zfa build, dart format lib/, dart fix --apply lib/), re-proves the suite green, and appends refactor evidence to cycle-log.md.
  referee         CI referee: the golden workflow verdict, the publishing gate, and the provenance rollup (spec 070).
  replay          Replay a feature's recorded TDD history in a clean sandbox: chain integrity, gen artifact compare, green verify. Clean = silent pass; divergence = the step named.
  reset           Revert a feature's TDD state to clean: drop the artifact registry entries and the generated tests/subjects the registry owns, reset run-state, and NEVER touch foreign files (bug #840). Prints the diff summary before acting.
  run             Drive a feature through BOTH lanes of the two-cycle runner — run-engine (CORE+BOTH behaviors) then run-skin (SKIN+BOTH, gated on a green engine) — failing fast on the first red, resuming from tdd/run-state.json, writing both lane receipts and the unified journal entry (spec 1008 over the spec 049 driver).
  run-engine      Drive only the ENGINE lane (CORE+BOTH behaviors, the engine plan) through gen -> verify-red -> make -> refactor and write the 04-engine-receipt.json verdict (spec 1008, issue #1008).
  run-skin        Drive the SKIN lane, gated on a green engine receipt (spec 1008). SKIN lanes with adaptive_slots run the hand-written conformance cycle — contract slots, red-before-green witness, _XRaySkinHandEdit annotation (spec 1005); others run gen -> verify-red -> make -> refactor. Both write 04-skin-receipt.json.
  split           One-shot migration (issue #1000): read the legacy single-file tdd/test-list.md, classify every behavior CORE or SKIN (spec `## Lanes` declarations winning over the kind heuristic), and emit tdd/04-ENGINE.md + tdd/04-SKIN.md + tdd/04-CONTRACT.md + tdd/split-receipt.json; test-list.md becomes the lane meta-index.
  status          Read the unified journal (via JournalReader) and print the one-line verdicts: status: feature=<f> engine=<verdict> skin=<verdict> plus the journal verdict line <f> | engine ✅ d/t | skin ✅ d/t | mocks c/t | n violations (exit 0 iff both green; spec 1008 + spec 1113).
  theater         Open the read-only replay TUI for a feature's TDD journal: receipts + cycle-log rendered as a three-pane terminal UI (spec 1006, issue #1006).
  verdicts        Print the versioned verdict envelope schema every `zfa tdd` subcommand emits under --json (issue #969).
  verify          Run the mutation_test audit on the feature's registered behavior artifacts and write tdd/verification.md (spec 044, FR-012..023).
  verify-red      Prove the target test is honestly red (assertion failure), append the red evidence to tdd/cycle-log.md, and exit 0 — or name the dishonest failure class and exit non-zero (spec 046).
  view            Generate the deterministic minimal view for a widget-kind behavior from its declared Presentation layer contract and scenario literals — the view-builder generation surface of the pipeline (issue #939).
  wire            Wire a behavior's gen'd subject stub to its generated entity — the subject-implementation step of the entity pipeline (bug #610, epic 045 precondition 5).

Run "zfa help" to see global options.
```

## `zfa test`

```
Generate Tests

Usage: zfa test <subcommand> [arguments]
-h, --help       Print this usage information.
-o, --output     Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                 (defaults to "lib/src")
    --dry-run    Preview generated files without writing to disk
-f, --force      Overwrite existing files
-v, --verbose    Enable detailed logging
    --revert     Revert generated files (delete them)
-m, --methods    Comma-separated list of methods (get,create,update,delete,list,watch,getList,watchList)
                 (defaults to "")
-d, --domain     Domain folder for custom usecases
                 (defaults to "general")
    --json       Print the test self-certification envelope {entity, tests, compile, errors[], schema:1} as JSON

Available subcommands:
  create   Create a Test

Run "zfa help" to see global options.
```

## `zfa ui`

```
UI vocabulary authority commands (skin plugin): schema, validate, preview

Usage: zfa ui <subcommand> [arguments]
-h, --help    Print this usage information.

Available subcommands:
  preview    Render a UI payload in a preview harness window (macOS only)
  schema     Export the full UI component vocabulary as a versioned JSON Schema
  validate   Validate a UI payload against the vocabulary schema and structural rules

Run "zfa help" to see global options.
```

## `zfa update`

```
Check for updates and update the installed zfa CLI.

Usage: zfa update [arguments]
-h, --help       Print this usage information.
    --dry-run    Check for updates without installing.
    --force      Force reinstall the latest version even if already up to date.

Run "zfa help" to see global options.
```

## `zfa usecase`

```
Generate UseCases

Usage: zfa usecase <subcommand> [arguments]
-h, --help       Print this usage information.
-o, --output     Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                 (defaults to "lib/src")
    --dry-run    Preview generated files without writing to disk
-f, --force      Overwrite existing files
-v, --verbose    Enable detailed logging
    --revert     Revert generated files (delete them)

Available subcommands:
  create   Create a Clean Architecture UseCase — per-method verdicts (--json) and a proof receipt
  verify   Verify the generated usecases conform to the contract (signatures, variants, entity drift) — exit 1 with --> fix: lines on drift (spec 1119)

Run "zfa help" to see global options.
```

## `zfa validate`

```
Validate JSON configuration file

Usage: zfa validate [arguments]
-h, --help    Print this usage information.

Run "zfa help" to see global options.
```

## `zfa view`

```
Run the View Plugin generator

Usage: zfa view <subcommand> [arguments]
-h, --help             Print this usage information.
-o, --output           Output directory for generated files (fixed to lib/src in v5; custom values are ignored)
                       (defaults to "lib/src")
    --dry-run          Preview generated files without writing to disk
-f, --force            Overwrite existing files
-v, --verbose          Enable detailed logging
    --revert           Revert generated files (delete them)
-m, --methods          Comma-separated list of methods (get,create,update,delete,watch,getList,watchList)
                       (defaults to "get,update")
    --[no-]di          Generate with DI integration
                       (defaults to on)
    --[no-]state       Generate with State integration
    --[no-]v6-state    Generate v6 dual-layer state (DomainState + ViewState + DualLayerPresenter) and ControlledWidget/FragmentBuilder-based view
    --[no-]route       Generate route definitions for this view
    --[no-]xray        Generate with X-Ray integration
    --[no-]skin        Generate with the runtime skin-contract auditor wrap (issue #1102): the view getter mounts SkinContractAuditor with a starter k<ViewName>SkinRows contract, and the auditor kit (skin/skin_contract_auditor.dart) is emitted when missing.
    --anchor           A zfa: anchor contract id this view declares (repeatable; issue #1112, requires --skin). Each anchor gains its anchorExists contract row and a generated debugTap<PascalAnchor>() VM-service driver function.

Available subcommands:
  create     Create a Flutter View class
  custom     Create a custom Flutter view (non-entity based)
  register   Register entities as parameters in a View

Run "zfa help" to see global options.
```

## `zfa xray`

```
X-Ray debug tools (overlay, control deck)

Usage: zfa xray <subcommand> [arguments]
-h, --help    Print this usage information.

Available subcommands:
  check     Check a feature slice: every file declares its xray layer, the deck stays inside the slice boundary (spec 1115)
  deck      Generate X-Ray Control Deck registration from annotations or YAML
  disable   Disable X-Ray overlay
  enable    Enable X-Ray overlay on next app launch
  mock      Scaffold @XRayMock annotations onto generated usecases
  status    Show current X-Ray configuration status

Run "zfa help" to see global options.
```

## `zfa zap`

```
Zuraffa Agent Protocol (ZAP): open agent interop — conform, serve, schema (issue #809).

Usage: zfa zap <subcommand> [arguments]
-h, --help    Print this usage information.

Available subcommands:
  conform   Run the ZAP conformance suite: schema self-integrity, golden examples, the malformed-message rejection table, and the reference client session.
  schema    Print a ZAP JSON Schema (draft-07), or export the full published contract (5 schemas + 4 golden examples) with --export.
  serve     Run the ZAP host over stdio: read NDJSON missions/checkpoints on stdin, write evidence/receipts to stdout (NDJSON only; logs go to stderr). Exits 0 at stdin EOF.

Run "zfa help" to see global options.
```

