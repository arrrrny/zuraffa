// Generated by zfa for: Barcode
// ignore_for_file: no_logic_in_create_state
import 'package:flutter/material.dart';
import 'package:zuraffa_flutter/zuraffa_flutter.dart';

import '../../../domain/entities/barcode/barcode.dart';
import 'barcode_controller.dart';
import 'barcode_presenter.dart';

class BarcodeView extends CleanView {
  const BarcodeView({super.key, super.routeObserver, this.barcode, this.id});

  final Barcode? barcode;

  final String? id;

  @override
  State<BarcodeView> createState() {
    return _BarcodeViewState(BarcodeController(BarcodePresenter()));
  }
}

class _BarcodeViewState
    extends CleanViewState<BarcodeView, BarcodeController, void> {
  _BarcodeViewState(super.controller);

  @override
  void onInitState() {
    super.onInitState();
    widget.id != null ? controller.getBarcode(widget.id!) : null;
  }

  @override
  Widget get view {
    return Scaffold(
      key: globalKey,
      appBar: AppBar(title: const Text('Barcode')),
      body: ControlledWidgetBuilder<BarcodeController>(
        builder: (context, controller) {
          return Container();
        },
      ),
    );
  }
}
