// GENERATED - DO NOT EDIT
// Generated by zfa for: PriceAlert
import '../../domain/entities/enums/index.dart';
import '../../domain/entities/price_alert/price_alert.dart';

/// Mock data for PriceAlert
class PriceAlertMockData {
  static final List<PriceAlert> priceAlerts = [
    PriceAlert(
      id: 'id 1',
      listingId: 'listingId 1',
      listingTitle: 'listingTitle 1',
      listingImageUrl: 'listingImageUrl 1',
      targetPrice: 10.5,
      currentPrice: 10.5,
      type: FeedbackType.values[1 % FeedbackType.values.length],
      createdAt: DateTime.now().subtract(const Duration(days: 30)),
      notifiedAt: DateTime.now().subtract(const Duration(days: 30)),
      isActive: true,
    ),
    PriceAlert(
      id: 'id 2',
      listingId: 'listingId 2',
      listingTitle: 'listingTitle 2',
      listingImageUrl: 'listingImageUrl 2',
      targetPrice: 21.0,
      currentPrice: 21.0,
      type: FeedbackType.values[2 % FeedbackType.values.length],
      createdAt: DateTime.now().subtract(const Duration(days: 60)),
      notifiedAt: DateTime.now().subtract(const Duration(days: 60)),
      isActive: false,
    ),
    PriceAlert(
      id: 'id 3',
      listingId: 'listingId 3',
      listingTitle: null,
      listingImageUrl: null,
      targetPrice: 31.5,
      currentPrice: 31.5,
      type: FeedbackType.values[3 % FeedbackType.values.length],
      createdAt: DateTime.now().subtract(const Duration(days: 90)),
      notifiedAt: null,
      isActive: true,
    ),
  ];

  static PriceAlert get samplePriceAlert => priceAlerts.first;

  static List<PriceAlert> get sampleList => priceAlerts;

  static List<PriceAlert> get emptyList => <PriceAlert>[];

  static List<PriceAlert> get largePriceAlertList =>
      List.generate(100, (index) => _createPriceAlert(index + 1000));

  static PriceAlert _createPriceAlert(int seed) {
    return PriceAlert(
      id: 'id $seed',
      listingId: 'listingId $seed',
      listingTitle: seed % 3 == 0 ? null : 'listingTitle $seed',
      listingImageUrl: seed % 3 == 0 ? null : 'listingImageUrl $seed',
      targetPrice: seed * 10.5,
      currentPrice: seed * 10.5,
      type: FeedbackType.values[seed % FeedbackType.values.length],
      createdAt: DateTime.now().subtract(Duration(days: seed * 30)),
      notifiedAt: seed % 3 == 0
          ? null
          : DateTime.now().subtract(Duration(days: seed * 30)),
      isActive: seed % 2 == 1,
    );
  }
}

// END GENERATED
