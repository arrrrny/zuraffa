// GENERATED - DO NOT EDIT
// Generated by zfa for: ChatSession
import '../../domain/entities/chat_session/chat_session.dart';
import 'chat_message_mock_data.dart';
import 'listing_mock_data.dart';

/// Mock data for ChatSession
class ChatSessionMockData {
  static final List<ChatSession> chatSessions = [
    ChatSession(
      id: 'id 1',
      title: 'title 1',
      messages: [
        ChatMessageMockData.chatMessages[1],
        ChatMessageMockData.chatMessages[2],
        ChatMessageMockData.chatMessages[0],
      ],
      listing: ListingMockData.sampleListing,
      createdAt: DateTime.now().subtract(const Duration(days: 30)),
      updatedAt: DateTime.now().subtract(const Duration(days: 30)),
    ),
    ChatSession(
      id: 'id 2',
      title: 'title 2',
      messages: [
        ChatMessageMockData.chatMessages[2],
        ChatMessageMockData.chatMessages[0],
        ChatMessageMockData.chatMessages[1],
      ],
      listing: ListingMockData.sampleListing,
      createdAt: DateTime.now().subtract(const Duration(days: 60)),
      updatedAt: DateTime.now().subtract(const Duration(days: 60)),
    ),
    ChatSession(
      id: 'id 3',
      title: null,
      messages: [
        ChatMessageMockData.chatMessages[0],
        ChatMessageMockData.chatMessages[1],
        ChatMessageMockData.chatMessages[2],
      ],
      listing: null,
      createdAt: DateTime.now().subtract(const Duration(days: 90)),
      updatedAt: null,
    ),
  ];

  static ChatSession get sampleChatSession => chatSessions.first;

  static List<ChatSession> get sampleList => chatSessions;

  static List<ChatSession> get emptyList => <ChatSession>[];

  static List<ChatSession> get largeChatSessionList =>
      List.generate(100, (index) => _createChatSession(index + 1000));

  static ChatSession _createChatSession(int seed) {
    return ChatSession(
      id: 'id $seed',
      title: seed % 3 == 0 ? null : 'title $seed',
      messages: [
        ChatMessageMockData.chatMessages[seed % 3],
        ChatMessageMockData.chatMessages[seed + 1 % 3],
        ChatMessageMockData.chatMessages[seed + 2 % 3],
      ],
      listing: seed % 3 == 0 ? null : ListingMockData.sampleListing,
      createdAt: DateTime.now().subtract(Duration(days: seed * 30)),
      updatedAt: seed % 3 == 0
          ? null
          : DateTime.now().subtract(Duration(days: seed * 30)),
    );
  }
}

// END GENERATED
