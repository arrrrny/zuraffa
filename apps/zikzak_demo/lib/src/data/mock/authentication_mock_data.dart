// GENERATED - DO NOT EDIT
// Generated by zfa for: Authentication
import '../../domain/entities/authentication/authentication.dart';
import '../../domain/entities/enums/index.dart';

/// Mock data for Authentication
class AuthenticationMockData {
  static final List<Authentication> authentications = [
    Authentication(
      id: 'id 1',
      userId: 'userId 1',
      method:
          AuthenticationMethod.values[1 % AuthenticationMethod.values.length],
      accessToken: 'accessToken 1',
      refreshToken: 'refreshToken 1',
      expiresAt: DateTime.now().subtract(const Duration(days: 30)),
      createdAt: DateTime.now().subtract(const Duration(days: 30)),
    ),
    Authentication(
      id: 'id 2',
      userId: 'userId 2',
      method:
          AuthenticationMethod.values[2 % AuthenticationMethod.values.length],
      accessToken: 'accessToken 2',
      refreshToken: 'refreshToken 2',
      expiresAt: DateTime.now().subtract(const Duration(days: 60)),
      createdAt: DateTime.now().subtract(const Duration(days: 60)),
    ),
    Authentication(
      id: 'id 3',
      userId: 'userId 3',
      method:
          AuthenticationMethod.values[3 % AuthenticationMethod.values.length],
      accessToken: null,
      refreshToken: null,
      expiresAt: null,
      createdAt: DateTime.now().subtract(const Duration(days: 90)),
    ),
  ];

  static Authentication get sampleAuthentication => authentications.first;

  static List<Authentication> get sampleList => authentications;

  static List<Authentication> get emptyList => <Authentication>[];

  static List<Authentication> get largeAuthenticationList =>
      List.generate(100, (index) => _createAuthentication(index + 1000));

  static Authentication _createAuthentication(int seed) {
    return Authentication(
      id: 'id $seed',
      userId: 'userId $seed',
      method: AuthenticationMethod
          .values[seed % AuthenticationMethod.values.length],
      accessToken: seed % 3 == 0 ? null : 'accessToken $seed',
      refreshToken: seed % 3 == 0 ? null : 'refreshToken $seed',
      expiresAt: seed % 3 == 0
          ? null
          : DateTime.now().subtract(Duration(days: seed * 30)),
      createdAt: DateTime.now().subtract(Duration(days: seed * 30)),
    );
  }
}

// END GENERATED
