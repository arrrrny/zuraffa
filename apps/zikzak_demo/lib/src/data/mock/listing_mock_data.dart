// GENERATED - DO NOT EDIT
// Generated by zfa for: Listing
import '../../domain/entities/listing/listing.dart';
import 'listing_offer_mock_data.dart';

/// Mock data for Listing
class ListingMockData {
  static final List<Listing> listings = [
    Listing(
      id: 'id 1',
      title: 'title 1',
      offers: [
        ListingOfferMockData.listingOffers[1],
        ListingOfferMockData.listingOffers[2],
        ListingOfferMockData.listingOffers[0],
      ],
      similarListings: [
        ListingMockData.listings[1],
        ListingMockData.listings[2],
        ListingMockData.listings[0],
      ],
      variations: [
        ListingMockData.listings[1],
        ListingMockData.listings[2],
        ListingMockData.listings[0],
      ],
      hasGoogleShoppingMatch: true,
      createdAt: DateTime.now().subtract(const Duration(days: 30)),
      updatedAt: DateTime.now().subtract(const Duration(days: 30)),
      groupId: 'groupId 1',
      reviewSummary: 'reviewSummary 1',
      enableAskZikZak: true,
      colorName: 'colorName 1',
      thumbnailUrl: 'thumbnailUrl 1',
    ),
    Listing(
      id: 'id 2',
      title: 'title 2',
      offers: [
        ListingOfferMockData.listingOffers[2],
        ListingOfferMockData.listingOffers[0],
        ListingOfferMockData.listingOffers[1],
      ],
      similarListings: [
        ListingMockData.listings[2],
        ListingMockData.listings[0],
        ListingMockData.listings[1],
      ],
      variations: [
        ListingMockData.listings[2],
        ListingMockData.listings[0],
        ListingMockData.listings[1],
      ],
      hasGoogleShoppingMatch: false,
      createdAt: DateTime.now().subtract(const Duration(days: 60)),
      updatedAt: DateTime.now().subtract(const Duration(days: 60)),
      groupId: 'groupId 2',
      reviewSummary: 'reviewSummary 2',
      enableAskZikZak: false,
      colorName: 'colorName 2',
      thumbnailUrl: 'thumbnailUrl 2',
    ),
    Listing(
      id: 'id 3',
      title: 'title 3',
      offers: null,
      similarListings: null,
      variations: null,
      hasGoogleShoppingMatch: null,
      createdAt: null,
      updatedAt: null,
      groupId: null,
      reviewSummary: null,
      enableAskZikZak: true,
      colorName: null,
      thumbnailUrl: null,
    ),
  ];

  static Listing get sampleListing => listings.first;

  static List<Listing> get sampleList => listings;

  static List<Listing> get emptyList => <Listing>[];

  static List<Listing> get largeListingList =>
      List.generate(100, (index) => _createListing(index + 1000));

  static Listing _createListing(int seed) {
    return Listing(
      id: 'id $seed',
      title: 'title $seed',
      offers: seed % 3 == 0
          ? null
          : [
              ListingOfferMockData.listingOffers[seed % 3],
              ListingOfferMockData.listingOffers[seed + 1 % 3],
              ListingOfferMockData.listingOffers[seed + 2 % 3],
            ],
      similarListings: seed % 3 == 0
          ? null
          : [
              ListingMockData.listings[seed % 3],
              ListingMockData.listings[seed + 1 % 3],
              ListingMockData.listings[seed + 2 % 3],
            ],
      variations: seed % 3 == 0
          ? null
          : [
              ListingMockData.listings[seed % 3],
              ListingMockData.listings[seed + 1 % 3],
              ListingMockData.listings[seed + 2 % 3],
            ],
      hasGoogleShoppingMatch: seed % 3 == 0 ? null : seed % 2 == 1,
      createdAt: seed % 3 == 0
          ? null
          : DateTime.now().subtract(Duration(days: seed * 30)),
      updatedAt: seed % 3 == 0
          ? null
          : DateTime.now().subtract(Duration(days: seed * 30)),
      groupId: seed % 3 == 0 ? null : 'groupId $seed',
      reviewSummary: seed % 3 == 0 ? null : 'reviewSummary $seed',
      enableAskZikZak: seed % 2 == 1,
      colorName: seed % 3 == 0 ? null : 'colorName $seed',
      thumbnailUrl: seed % 3 == 0 ? null : 'thumbnailUrl $seed',
    );
  }
}

// END GENERATED
