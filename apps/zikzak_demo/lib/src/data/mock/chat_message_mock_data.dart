// GENERATED - DO NOT EDIT
// Generated by zfa for: ChatMessage
import '../../domain/entities/chat_message/chat_message.dart';
import '../../domain/entities/enums/index.dart';
import 'listing_mock_data.dart';

/// Mock data for ChatMessage
class ChatMessageMockData {
  static final List<ChatMessage> chatMessages = [
    ChatMessage(
      id: 'id 1',
      role: ChatMessageRole.values[1 % ChatMessageRole.values.length],
      content: 'content 1',
      timestamp: DateTime.now().subtract(const Duration(days: 30)),
      products: [
        ListingMockData.listings[1],
        ListingMockData.listings[2],
        ListingMockData.listings[0],
      ],
    ),
    ChatMessage(
      id: 'id 2',
      role: ChatMessageRole.values[2 % ChatMessageRole.values.length],
      content: 'content 2',
      timestamp: DateTime.now().subtract(const Duration(days: 60)),
      products: [
        ListingMockData.listings[2],
        ListingMockData.listings[0],
        ListingMockData.listings[1],
      ],
    ),
    ChatMessage(
      id: 'id 3',
      role: ChatMessageRole.values[3 % ChatMessageRole.values.length],
      content: 'content 3',
      timestamp: DateTime.now().subtract(const Duration(days: 90)),
      products: null,
    ),
  ];

  static ChatMessage get sampleChatMessage => chatMessages.first;

  static List<ChatMessage> get sampleList => chatMessages;

  static List<ChatMessage> get emptyList => <ChatMessage>[];

  static List<ChatMessage> get largeChatMessageList =>
      List.generate(100, (index) => _createChatMessage(index + 1000));

  static ChatMessage _createChatMessage(int seed) {
    return ChatMessage(
      id: 'id $seed',
      role: ChatMessageRole.values[seed % ChatMessageRole.values.length],
      content: 'content $seed',
      timestamp: DateTime.now().subtract(Duration(days: seed * 30)),
      products: seed % 3 == 0
          ? null
          : [
              ListingMockData.listings[seed % 3],
              ListingMockData.listings[seed + 1 % 3],
              ListingMockData.listings[seed + 2 % 3],
            ],
    );
  }
}

// END GENERATED
