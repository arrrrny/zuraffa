// GENERATED - DO NOT EDIT
// Generated by zfa for: Authentication
import 'dart:async';

import 'package:zuraffa/zuraffa.dart';

import '../../../domain/entities/authentication/authentication.dart';
import '../../mock/authentication_mock_data.dart';
import 'authentication_datasource.dart';

/// Mock data source for Authentication
class AuthenticationMockDataSource
    with Loggable, FailureHandler
    implements AuthenticationDataSource {
  AuthenticationMockDataSource([Duration? delay])
    : _delay = delay ?? const Duration(milliseconds: 100);

  final Duration _delay;

  @override
  Future<Authentication> get(QueryParams<Authentication> params) async {
    logger.info('Getting Authentication with params: $params');
    await Future.delayed(_delay);
    final item = AuthenticationMockData.authentications.query(params);
    logger.info('Successfully retrieved Authentication');
    return item;
  }

  @override
  Future<Authentication> update(
    UpdateParams<String, AuthenticationPatch> params,
  ) async {
    logger.info('Updating Authentication: ${params.id}');
    await Future.delayed(_delay);
    final existing = AuthenticationMockData.sampleAuthentication;
    logger.info('Successfully updated Authentication');
    return existing;
  }

  @override
  Future<Authentication> toggle(
    ToggleParams<String, Field<Authentication, dynamic>> params,
  ) async {
    logger.info(
      'Toggling Authentication: ${params.id} on field ${params.field}',
    );
    await Future.delayed(_delay);
    final existing = AuthenticationMockData.sampleAuthentication;
    logger.info('Successfully toggled Authentication');
    return existing;
  }
}

// END GENERATED
