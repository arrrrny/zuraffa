// GENERATED - DO NOT EDIT
// Generated by zfa for: ChatMessage
import 'dart:async';

import 'package:zuraffa/zuraffa.dart';

import '../../../domain/entities/chat_message/chat_message.dart';
import '../../mock/chat_message_mock_data.dart';
import 'chat_message_datasource.dart';

/// Mock data source for ChatMessage
class ChatMessageMockDataSource
    with Loggable, FailureHandler
    implements ChatMessageDataSource {
  ChatMessageMockDataSource([Duration? delay])
    : _delay = delay ?? const Duration(milliseconds: 100);

  final Duration _delay;

  @override
  Future<ChatMessage> get(QueryParams<ChatMessage> params) async {
    logger.info('Getting ChatMessage with params: $params');
    await Future.delayed(_delay);
    final item = ChatMessageMockData.chatMessages.query(params);
    logger.info('Successfully retrieved ChatMessage');
    return item;
  }

  @override
  Future<ChatMessage> update(
    UpdateParams<String, ChatMessagePatch> params,
  ) async {
    logger.info('Updating ChatMessage: ${params.id}');
    await Future.delayed(_delay);
    final existing = ChatMessageMockData.sampleChatMessage;
    logger.info('Successfully updated ChatMessage');
    return existing;
  }

  @override
  Future<ChatMessage> toggle(
    ToggleParams<String, Field<ChatMessage, dynamic>> params,
  ) async {
    logger.info('Toggling ChatMessage: ${params.id} on field ${params.field}');
    await Future.delayed(_delay);
    final existing = ChatMessageMockData.sampleChatMessage;
    logger.info('Successfully toggled ChatMessage');
    return existing;
  }
}

// END GENERATED
