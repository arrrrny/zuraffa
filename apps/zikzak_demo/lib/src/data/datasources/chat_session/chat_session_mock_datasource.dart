// GENERATED - DO NOT EDIT
// Generated by zfa for: ChatSession
import 'dart:async';

import 'package:zuraffa/zuraffa.dart';

import '../../../domain/entities/chat_session/chat_session.dart';
import '../../mock/chat_session_mock_data.dart';
import 'chat_session_datasource.dart';

/// Mock data source for ChatSession
class ChatSessionMockDataSource
    with Loggable, FailureHandler
    implements ChatSessionDataSource {
  ChatSessionMockDataSource([Duration? delay])
    : _delay = delay ?? const Duration(milliseconds: 100);

  final Duration _delay;

  @override
  Future<ChatSession> get(QueryParams<ChatSession> params) async {
    logger.info('Getting ChatSession with params: $params');
    await Future.delayed(_delay);
    final item = ChatSessionMockData.chatSessions.query(params);
    logger.info('Successfully retrieved ChatSession');
    return item;
  }

  @override
  Future<ChatSession> update(
    UpdateParams<String, ChatSessionPatch> params,
  ) async {
    logger.info('Updating ChatSession: ${params.id}');
    await Future.delayed(_delay);
    final existing = ChatSessionMockData.sampleChatSession;
    logger.info('Successfully updated ChatSession');
    return existing;
  }

  @override
  Future<ChatSession> toggle(
    ToggleParams<String, Field<ChatSession, dynamic>> params,
  ) async {
    logger.info('Toggling ChatSession: ${params.id} on field ${params.field}');
    await Future.delayed(_delay);
    final existing = ChatSessionMockData.sampleChatSession;
    logger.info('Successfully toggled ChatSession');
    return existing;
  }
}

// END GENERATED
