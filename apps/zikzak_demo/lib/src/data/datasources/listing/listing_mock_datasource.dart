// GENERATED - DO NOT EDIT
// Generated by zfa for: Listing
import 'dart:async';

import 'package:zuraffa/zuraffa.dart';

import '../../../domain/entities/listing/listing.dart';
import '../../mock/listing_mock_data.dart';
import 'listing_datasource.dart';

/// Mock data source for Listing
class ListingMockDataSource
    with Loggable, FailureHandler
    implements ListingDataSource {
  ListingMockDataSource([Duration? delay])
    : _delay = delay ?? const Duration(milliseconds: 100);

  final Duration _delay;

  @override
  Future<Listing> get(QueryParams<Listing> params) async {
    logger.info('Getting Listing with params: $params');
    await Future.delayed(_delay);
    final item = ListingMockData.listings.query(params);
    logger.info('Successfully retrieved Listing');
    return item;
  }

  @override
  Future<Listing> update(UpdateParams<String, ListingPatch> params) async {
    logger.info('Updating Listing: ${params.id}');
    await Future.delayed(_delay);
    final existing = ListingMockData.sampleListing;
    logger.info('Successfully updated Listing');
    return existing;
  }

  @override
  Future<Listing> toggle(
    ToggleParams<String, Field<Listing, dynamic>> params,
  ) async {
    logger.info('Toggling Listing: ${params.id} on field ${params.field}');
    await Future.delayed(_delay);
    final existing = ListingMockData.sampleListing;
    logger.info('Successfully toggled Listing');
    return existing;
  }
}

// END GENERATED
