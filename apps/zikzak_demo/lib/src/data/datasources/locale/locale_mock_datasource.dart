// GENERATED - DO NOT EDIT
// Generated by zfa for: Locale
import 'dart:async';

import 'package:zuraffa/zuraffa.dart';

import '../../../domain/entities/locale/locale.dart';
import '../../mock/locale_mock_data.dart';
import 'locale_datasource.dart';

/// Mock data source for Locale
class LocaleMockDataSource
    with Loggable, FailureHandler
    implements LocaleDataSource {
  LocaleMockDataSource([Duration? delay])
    : _delay = delay ?? const Duration(milliseconds: 100);

  final Duration _delay;

  @override
  Future<Locale> get(QueryParams<Locale> params) async {
    logger.info('Getting Locale with params: $params');
    await Future.delayed(_delay);
    final item = LocaleMockData.locales.query(params);
    logger.info('Successfully retrieved Locale');
    return item;
  }

  @override
  Future<Locale> update(UpdateParams<String, LocalePatch> params) async {
    logger.info('Updating Locale: ${params.id}');
    await Future.delayed(_delay);
    final existing = LocaleMockData.sampleLocale;
    logger.info('Successfully updated Locale');
    return existing;
  }

  @override
  Future<Locale> toggle(
    ToggleParams<String, Field<Locale, dynamic>> params,
  ) async {
    logger.info('Toggling Locale: ${params.id} on field ${params.field}');
    await Future.delayed(_delay);
    final existing = LocaleMockData.sampleLocale;
    logger.info('Successfully toggled Locale');
    return existing;
  }
}

// END GENERATED
