// GENERATED - DO NOT EDIT
// Generated by zfa for: Walkthrough
import 'dart:async';

import 'package:zuraffa/zuraffa.dart';

import '../../../domain/entities/walkthrough/walkthrough.dart';
import '../../mock/walkthrough_mock_data.dart';
import 'walkthrough_datasource.dart';

/// Mock data source for Walkthrough
class WalkthroughMockDataSource
    with Loggable, FailureHandler
    implements WalkthroughDataSource {
  WalkthroughMockDataSource([Duration? delay])
    : _delay = delay ?? const Duration(milliseconds: 100);

  final Duration _delay;

  @override
  Future<Walkthrough> get(QueryParams<Walkthrough> params) async {
    logger.info('Getting Walkthrough with params: $params');
    await Future.delayed(_delay);
    final item = WalkthroughMockData.walkthroughs.query(params);
    logger.info('Successfully retrieved Walkthrough');
    return item;
  }

  @override
  Future<Walkthrough> update(
    UpdateParams<String, WalkthroughPatch> params,
  ) async {
    logger.info('Updating Walkthrough: ${params.id}');
    await Future.delayed(_delay);
    final existing = WalkthroughMockData.sampleWalkthrough;
    logger.info('Successfully updated Walkthrough');
    return existing;
  }

  @override
  Future<Walkthrough> toggle(
    ToggleParams<String, Field<Walkthrough, dynamic>> params,
  ) async {
    logger.info('Toggling Walkthrough: ${params.id} on field ${params.field}');
    await Future.delayed(_delay);
    final existing = WalkthroughMockData.sampleWalkthrough;
    logger.info('Successfully toggled Walkthrough');
    return existing;
  }
}

// END GENERATED
