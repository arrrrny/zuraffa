// GENERATED - DO NOT EDIT
// Generated by zfa for: DynamicForm
import 'dart:async';

import 'package:zuraffa/zuraffa.dart';

import '../../../domain/entities/dynamic_form/dynamic_form.dart';
import '../../mock/dynamic_form_mock_data.dart';
import 'dynamic_form_datasource.dart';

/// Mock data source for DynamicForm
class DynamicFormMockDataSource
    with Loggable, FailureHandler
    implements DynamicFormDataSource {
  DynamicFormMockDataSource([Duration? delay])
    : _delay = delay ?? const Duration(milliseconds: 100);

  final Duration _delay;

  @override
  Future<DynamicForm> get(QueryParams<DynamicForm> params) async {
    logger.info('Getting DynamicForm with params: $params');
    await Future.delayed(_delay);
    final item = DynamicFormMockData.dynamicForms.query(params);
    logger.info('Successfully retrieved DynamicForm');
    return item;
  }

  @override
  Future<DynamicForm> update(
    UpdateParams<String, DynamicFormPatch> params,
  ) async {
    logger.info('Updating DynamicForm: ${params.id}');
    await Future.delayed(_delay);
    final existing = DynamicFormMockData.sampleDynamicForm;
    logger.info('Successfully updated DynamicForm');
    return existing;
  }

  @override
  Future<DynamicForm> toggle(
    ToggleParams<String, Field<DynamicForm, dynamic>> params,
  ) async {
    logger.info('Toggling DynamicForm: ${params.id} on field ${params.field}');
    await Future.delayed(_delay);
    final existing = DynamicFormMockData.sampleDynamicForm;
    logger.info('Successfully toggled DynamicForm');
    return existing;
  }
}

// END GENERATED
