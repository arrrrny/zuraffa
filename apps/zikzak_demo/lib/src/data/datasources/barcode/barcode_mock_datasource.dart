// GENERATED - DO NOT EDIT
// Generated by zfa for: Barcode
import 'dart:async';

import 'package:zuraffa/zuraffa.dart';

import '../../../domain/entities/barcode/barcode.dart';
import '../../mock/barcode_mock_data.dart';
import 'barcode_datasource.dart';

/// Mock data source for Barcode
class BarcodeMockDataSource
    with Loggable, FailureHandler
    implements BarcodeDataSource {
  BarcodeMockDataSource([Duration? delay])
    : _delay = delay ?? const Duration(milliseconds: 100);

  final Duration _delay;

  @override
  Future<Barcode> get(QueryParams<Barcode> params) async {
    logger.info('Getting Barcode with params: $params');
    await Future.delayed(_delay);
    final item = BarcodeMockData.barcodes.query(params);
    logger.info('Successfully retrieved Barcode');
    return item;
  }

  @override
  Future<Barcode> update(UpdateParams<String, BarcodePatch> params) async {
    logger.info('Updating Barcode: ${params.id}');
    await Future.delayed(_delay);
    final existing = BarcodeMockData.sampleBarcode;
    logger.info('Successfully updated Barcode');
    return existing;
  }

  @override
  Future<Barcode> toggle(
    ToggleParams<String, Field<Barcode, dynamic>> params,
  ) async {
    logger.info('Toggling Barcode: ${params.id} on field ${params.field}');
    await Future.delayed(_delay);
    final existing = BarcodeMockData.sampleBarcode;
    logger.info('Successfully toggled Barcode');
    return existing;
  }
}

// END GENERATED
