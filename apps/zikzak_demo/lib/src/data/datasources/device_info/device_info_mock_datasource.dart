// GENERATED - DO NOT EDIT
// Generated by zfa for: DeviceInfo
import 'dart:async';

import 'package:zuraffa/zuraffa.dart';

import '../../../domain/entities/device_info/device_info.dart';
import '../../mock/device_info_mock_data.dart';
import 'device_info_datasource.dart';

/// Mock data source for DeviceInfo
class DeviceInfoMockDataSource
    with Loggable, FailureHandler
    implements DeviceInfoDataSource {
  DeviceInfoMockDataSource([Duration? delay])
    : _delay = delay ?? const Duration(milliseconds: 100);

  final Duration _delay;

  @override
  Future<DeviceInfo> get(QueryParams<DeviceInfo> params) async {
    logger.info('Getting DeviceInfo with params: $params');
    await Future.delayed(_delay);
    final item = DeviceInfoMockData.deviceInfos.query(params);
    logger.info('Successfully retrieved DeviceInfo');
    return item;
  }

  @override
  Future<DeviceInfo> update(
    UpdateParams<String, DeviceInfoPatch> params,
  ) async {
    logger.info('Updating DeviceInfo: ${params.id}');
    await Future.delayed(_delay);
    final existing = DeviceInfoMockData.sampleDeviceInfo;
    logger.info('Successfully updated DeviceInfo');
    return existing;
  }

  @override
  Future<DeviceInfo> toggle(
    ToggleParams<String, Field<DeviceInfo, dynamic>> params,
  ) async {
    logger.info('Toggling DeviceInfo: ${params.id} on field ${params.field}');
    await Future.delayed(_delay);
    final existing = DeviceInfoMockData.sampleDeviceInfo;
    logger.info('Successfully toggled DeviceInfo');
    return existing;
  }
}

// END GENERATED
