// GENERATED - DO NOT EDIT
// Generated by zfa for: UrlSpark
import 'dart:async';

import 'package:zuraffa/zuraffa.dart';

import '../../../domain/entities/url_spark/url_spark.dart';
import '../../mock/url_spark_mock_data.dart';
import 'url_spark_datasource.dart';

/// Mock data source for UrlSpark
class UrlSparkMockDataSource
    with Loggable, FailureHandler
    implements UrlSparkDataSource {
  UrlSparkMockDataSource([Duration? delay])
    : _delay = delay ?? const Duration(milliseconds: 100);

  final Duration _delay;

  @override
  Future<UrlSpark> get(QueryParams<UrlSpark> params) async {
    logger.info('Getting UrlSpark with params: $params');
    await Future.delayed(_delay);
    final item = UrlSparkMockData.urlSparks.query(params);
    logger.info('Successfully retrieved UrlSpark');
    return item;
  }

  @override
  Future<UrlSpark> update(UpdateParams<String, UrlSparkPatch> params) async {
    logger.info('Updating UrlSpark: ${params.id}');
    await Future.delayed(_delay);
    final existing = UrlSparkMockData.sampleUrlSpark;
    logger.info('Successfully updated UrlSpark');
    return existing;
  }

  @override
  Future<UrlSpark> toggle(
    ToggleParams<String, Field<UrlSpark, dynamic>> params,
  ) async {
    logger.info('Toggling UrlSpark: ${params.id} on field ${params.field}');
    await Future.delayed(_delay);
    final existing = UrlSparkMockData.sampleUrlSpark;
    logger.info('Successfully toggled UrlSpark');
    return existing;
  }
}

// END GENERATED
