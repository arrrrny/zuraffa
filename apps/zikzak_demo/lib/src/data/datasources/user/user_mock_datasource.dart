// GENERATED - DO NOT EDIT
// Generated by zfa for: User
import 'dart:async';

import 'package:zuraffa/zuraffa.dart';

import '../../../domain/entities/user/user.dart';
import '../../mock/user_mock_data.dart';
import 'user_datasource.dart';

/// Mock data source for User
class UserMockDataSource
    with Loggable, FailureHandler
    implements UserDataSource {
  UserMockDataSource([Duration? delay])
    : _delay = delay ?? const Duration(milliseconds: 100);

  final Duration _delay;

  @override
  Future<User> get(QueryParams<User> params) async {
    logger.info('Getting User with params: $params');
    await Future.delayed(_delay);
    final item = UserMockData.users.query(params);
    logger.info('Successfully retrieved User');
    return item;
  }

  @override
  Future<User> update(UpdateParams<String, UserPatch> params) async {
    logger.info('Updating User: ${params.id}');
    await Future.delayed(_delay);
    final existing = UserMockData.sampleUser;
    logger.info('Successfully updated User');
    return existing;
  }

  @override
  Future<User> toggle(ToggleParams<String, Field<User, dynamic>> params) async {
    logger.info('Toggling User: ${params.id} on field ${params.field}');
    await Future.delayed(_delay);
    final existing = UserMockData.sampleUser;
    logger.info('Successfully toggled User');
    return existing;
  }
}

// END GENERATED
