// GENERATED - DO NOT EDIT
// Generated by zfa for: MetricDetail
import 'dart:async';

import 'package:zuraffa/zuraffa.dart';

import '../../../domain/entities/metric_detail/metric_detail.dart';
import '../../mock/metric_detail_mock_data.dart';
import 'metric_detail_datasource.dart';

/// Mock data source for MetricDetail
class MetricDetailMockDataSource
    with Loggable, FailureHandler
    implements MetricDetailDataSource {
  MetricDetailMockDataSource([Duration? delay])
    : _delay = delay ?? const Duration(milliseconds: 100);

  final Duration _delay;

  @override
  Future<MetricDetail> get(QueryParams<MetricDetail> params) async {
    logger.info('Getting MetricDetail with params: $params');
    await Future.delayed(_delay);
    final item = MetricDetailMockData.metricDetails.query(params);
    logger.info('Successfully retrieved MetricDetail');
    return item;
  }

  @override
  Future<MetricDetail> update(
    UpdateParams<String, MetricDetailPatch> params,
  ) async {
    logger.info('Updating MetricDetail: ${params.id}');
    await Future.delayed(_delay);
    final existing = MetricDetailMockData.sampleMetricDetail;
    logger.info('Successfully updated MetricDetail');
    return existing;
  }

  @override
  Future<MetricDetail> toggle(
    ToggleParams<String, Field<MetricDetail, dynamic>> params,
  ) async {
    logger.info('Toggling MetricDetail: ${params.id} on field ${params.field}');
    await Future.delayed(_delay);
    final existing = MetricDetailMockData.sampleMetricDetail;
    logger.info('Successfully toggled MetricDetail');
    return existing;
  }
}

// END GENERATED
