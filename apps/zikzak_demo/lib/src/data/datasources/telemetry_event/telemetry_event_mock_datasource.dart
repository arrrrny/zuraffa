// GENERATED - DO NOT EDIT
// Generated by zfa for: TelemetryEvent
import 'dart:async';

import 'package:zuraffa/zuraffa.dart';

import '../../../domain/entities/telemetry_event/telemetry_event.dart';
import '../../mock/telemetry_event_mock_data.dart';
import 'telemetry_event_datasource.dart';

/// Mock data source for TelemetryEvent
class TelemetryEventMockDataSource
    with Loggable, FailureHandler
    implements TelemetryEventDataSource {
  TelemetryEventMockDataSource([Duration? delay])
    : _delay = delay ?? const Duration(milliseconds: 100);

  final Duration _delay;

  @override
  Future<TelemetryEvent> get(QueryParams<TelemetryEvent> params) async {
    logger.info('Getting TelemetryEvent with params: $params');
    await Future.delayed(_delay);
    final item = TelemetryEventMockData.telemetryEvents.query(params);
    logger.info('Successfully retrieved TelemetryEvent');
    return item;
  }

  @override
  Future<TelemetryEvent> update(
    UpdateParams<String, TelemetryEventPatch> params,
  ) async {
    logger.info('Updating TelemetryEvent: ${params.id}');
    await Future.delayed(_delay);
    final existing = TelemetryEventMockData.sampleTelemetryEvent;
    logger.info('Successfully updated TelemetryEvent');
    return existing;
  }

  @override
  Future<TelemetryEvent> toggle(
    ToggleParams<String, Field<TelemetryEvent, dynamic>> params,
  ) async {
    logger.info(
      'Toggling TelemetryEvent: ${params.id} on field ${params.field}',
    );
    await Future.delayed(_delay);
    final existing = TelemetryEventMockData.sampleTelemetryEvent;
    logger.info('Successfully toggled TelemetryEvent');
    return existing;
  }
}

// END GENERATED
