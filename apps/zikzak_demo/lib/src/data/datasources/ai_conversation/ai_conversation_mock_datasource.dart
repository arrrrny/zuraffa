// GENERATED - DO NOT EDIT
// Generated by zfa for: AiConversation
import 'dart:async';

import 'package:zuraffa/zuraffa.dart';

import '../../../domain/entities/ai_conversation/ai_conversation.dart';
import '../../mock/ai_conversation_mock_data.dart';
import 'ai_conversation_datasource.dart';

/// Mock data source for AiConversation
class AiConversationMockDataSource
    with Loggable, FailureHandler
    implements AiConversationDataSource {
  AiConversationMockDataSource([Duration? delay])
    : _delay = delay ?? const Duration(milliseconds: 100);

  final Duration _delay;

  @override
  Future<AiConversation> get(QueryParams<AiConversation> params) async {
    logger.info('Getting AiConversation with params: $params');
    await Future<void>.delayed(_delay);
    final item = AiConversationMockData.aiConversations.query(params);
    logger.info('Successfully retrieved AiConversation');
    return item;
  }

  @override
  Future<AiConversation> update(
    UpdateParams<String, AiConversationPatch> params,
  ) async {
    logger.info('Updating AiConversation: ${params.id}');
    await Future<void>.delayed(_delay);
    final existing = AiConversationMockData.aiConversations.firstWhere(
      (item) => item.id == params.id,
      orElse: () =>
          throw notFoundFailure('AiConversation not found in mock data'),
    );
    final updated = params.data.applyTo(existing);
    final index = AiConversationMockData.aiConversations.indexOf(existing);
    AiConversationMockData.aiConversations[index] = updated;
    logger.info('Successfully updated AiConversation');
    return updated;
  }

  @override
  Future<AiConversation> toggle(
    ToggleParams<String, Field<AiConversation, dynamic>> params,
  ) async {
    logger.info(
      'Toggling AiConversation: ${params.id} on field ${params.field}',
    );
    await Future<void>.delayed(_delay);
    final existing = AiConversationMockData.aiConversations.firstWhere(
      (item) => item.id == params.id,
      orElse: () =>
          throw notFoundFailure('AiConversation not found in mock data'),
    );
    final updated = existing.copyWithField(params.field, params.value);
    final index = AiConversationMockData.aiConversations.indexOf(existing);
    AiConversationMockData.aiConversations[index] = updated;
    logger.info('Successfully toggled AiConversation');
    return updated;
  }
}

// END GENERATED
