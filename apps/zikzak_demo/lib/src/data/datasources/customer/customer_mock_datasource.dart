// GENERATED - DO NOT EDIT
// Generated by zfa for: Customer
import 'dart:async';

import 'package:zuraffa/zuraffa.dart';

import '../../../domain/entities/customer/customer.dart';
import '../../mock/customer_mock_data.dart';
import 'customer_datasource.dart';

/// Mock data source for Customer
class CustomerMockDataSource
    with Loggable, FailureHandler
    implements CustomerDataSource {
  CustomerMockDataSource([Duration? delay])
    : _delay = delay ?? const Duration(milliseconds: 100);

  final Duration _delay;

  @override
  Future<Customer> get(QueryParams<Customer> params) async {
    logger.info('Getting Customer with params: $params');
    await Future.delayed(_delay);
    final item = CustomerMockData.customers.query(params);
    logger.info('Successfully retrieved Customer');
    return item;
  }

  @override
  Future<Customer> update(UpdateParams<String, CustomerPatch> params) async {
    logger.info('Updating Customer: ${params.id}');
    await Future.delayed(_delay);
    final existing = CustomerMockData.sampleCustomer;
    logger.info('Successfully updated Customer');
    return existing;
  }

  @override
  Future<Customer> toggle(
    ToggleParams<String, Field<Customer, dynamic>> params,
  ) async {
    logger.info('Toggling Customer: ${params.id} on field ${params.field}');
    await Future.delayed(_delay);
    final existing = CustomerMockData.sampleCustomer;
    logger.info('Successfully toggled Customer');
    return existing;
  }
}

// END GENERATED
