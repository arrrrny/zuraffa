// GENERATED - DO NOT EDIT
// Generated by zfa for: FormSubmission
import 'dart:async';

import 'package:zuraffa/zuraffa.dart';

import '../../../domain/entities/form_submission/form_submission.dart';
import '../../mock/form_submission_mock_data.dart';
import 'form_submission_datasource.dart';

/// Mock data source for FormSubmission
class FormSubmissionMockDataSource
    with Loggable, FailureHandler
    implements FormSubmissionDataSource {
  FormSubmissionMockDataSource([Duration? delay])
    : _delay = delay ?? const Duration(milliseconds: 100);

  final Duration _delay;

  @override
  Future<FormSubmission> get(QueryParams<FormSubmission> params) async {
    logger.info('Getting FormSubmission with params: $params');
    await Future.delayed(_delay);
    final item = FormSubmissionMockData.formSubmissions.query(params);
    logger.info('Successfully retrieved FormSubmission');
    return item;
  }

  @override
  Future<FormSubmission> update(
    UpdateParams<String, FormSubmissionPatch> params,
  ) async {
    logger.info('Updating FormSubmission: ${params.id}');
    await Future.delayed(_delay);
    final existing = FormSubmissionMockData.sampleFormSubmission;
    logger.info('Successfully updated FormSubmission');
    return existing;
  }

  @override
  Future<FormSubmission> toggle(
    ToggleParams<String, Field<FormSubmission, dynamic>> params,
  ) async {
    logger.info(
      'Toggling FormSubmission: ${params.id} on field ${params.field}',
    );
    await Future.delayed(_delay);
    final existing = FormSubmissionMockData.sampleFormSubmission;
    logger.info('Successfully toggled FormSubmission');
    return existing;
  }
}

// END GENERATED
