# Test List: template-self-hosting (issue #912)

## Acceptance Behaviors

- [ ] A1: `BehaviorTestWriter` escapes apostrophes in persistence test descriptions (e.g. `user's theme`) so the generated Dart file is syntactically valid.
- [ ] A2: `zfa tdd migrate-paths` rewrites package-URI subject imports when moving tests to namespaced layout.
