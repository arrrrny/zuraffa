# Cycle Log: bug #610 — tdd make never wires the subject (green unreachable
# with the real pipeline)

Append only. Newest last. Every entry's `red` block is the evidence that
the test existed and failed before the implementation.

## Baseline

- suite: tdd plugin `--preset=all` chunked -> all green on the #609 branch
  (345 passed pre-#609-tests; post-#609 the planner pin + drift guard add 7)
- commit: `db00ff5e` (fix/610 branch base = the #609 fix on top of the
  compile-repair base `00ed1451`)
- recorded: cycle 0, before any change

## Cycle 1: the entity plan gains the subject-implementation step

- behavior: an entity-bearing behavior's REAL pipeline run (no fake zfa)
  certifies green — the gen'd subject stub is implemented by a pipeline
  step, never by a wrapper or by hand (epic 045 precondition 5, FR-003).
- design decision (step 2 of the cycle, resolved before RED):
  `zfa tdd wire <id> --entity <Name>` over `zfa make --with=tdd-subject`.
  Basis: subject-contract ownership (tdd plugin owns SubjectWriter/registry
  artifacts; core make would need a forbidden core→plugin import),
  provenance clarity (045 FR-005 — a dedicated self-describing invocation
  is the attribution record), and public-CLI stability. Recorded in the
  WireCommand doc comment, the PR body, and the epic 045 harness spec
  (precondition 5).
- test: `test/plugins/tdd/scenarios/sc_017_real_pipeline_wires_subject_test.dart`
  (new, slow+integration tier; the assessment's "anchor test"): a real
  buildable fixture (zorphy + json deps, real `dart pub get`), a real
  test-list row, then the real `zfa tdd gen` → `zfa tdd verify-red` →
  `zfa tdd make` with `--zfa-bin` pointing at a pure exec forwarder to the
  real `bin/zfa.dart` (transport only, no generation semantics).
- red: `dart test --preset=integration
  test/plugins/tdd/scenarios/sc_017_real_pipeline_wires_subject_test.dart`
  -> FAILED in 1:47 with exactly the bug's production signature:
  `zfa tdd make: target test still fails after generation (exit 1).` /
  `make: behavior=B-001 outcome=generation-error feature=090-tdd-fixture`
  — the REAL `entity create` and REAL `build` both succeeded, the subject
  was never wired, the target test stayed red, make honestly stopped.
- green: planner emits the wire step (`['tdd', 'wire', <id>, '--entity',
  <Name>]`) between create and build; new `WireCommand`
  (`lib/src/plugins/tdd/commands/wire_command.dart`, registered in
  TddCommand) implements the stub against the generated entity. Re-run:
  sc_017 PASSED in 2:06 — `outcome=green`, exit 0, subject contains no
  `UnimplementedError` and imports the entity, green evidence records all
  three real generation commands, zorphy parts generated by the real
  build.
- companion tests: planner U6 updated to pin the 3-step plan + wire argv
  (fast); new `wire_command_test.dart` (6 fast cases: wired rewrite,
  already-wired idempotency, missing subject, missing entity, unrecognized
  stub shape refused, --entity required); sc_005's fake-pipeline replay
  updated 2 -> 3 recorded invocations (wire is now in the plan).
- refactor: none required.
- deliberate mutant (mutation sampling, no mutation tool in profile):
  replaying the gap — removing the wire step from the plan — is exactly
  the recorded red (sc_017 fails with generation-error). 1 mutant, 1
  caught; restoring the step returns sc_017 and the plugin suite to green.
- commit: (this commit)
