# tdd.verify — EPIC 1133 TDD Loop Completeness — the epic-closing lane

- **Verified**: 2026-09-18, this session, on
  `feat/1133-tdd-loop-completeness` (working tree, pre-push), master @
  a9329746
- **Toolchain**: Dart 3.13.4 / Flutter 3.47.4 stable on linux_x64 (the
  SPEC's floor: Dart 3.13+ / Flutter 3.47+)
- **Scope**: `example/lib/tdd/004-login-ui/u1_subject.dart` (the green),
  `example/specs/004-login-ui/tdd/{cycle-log.md,verification.md}` (the
  evidence), the epic record under
  `.specify/specs/1133-tdd-loop-completeness/` (spec/plan/tdd), and this
  lane's root `tdd/` artifacts. The four lanes' plugin code is the
  already-merged PR set (#1216, #1047/#1209, #1048/#1362, #1219; kind
  trace #1396) — unchanged by this lane.

## Verdict: PASS (all four epic exit criteria proved on current master)

Full detail with commands and hashes:
`.specify/specs/1133-tdd-loop-completeness/tdd/verification.md`.

1. **AC-4 route outcome** — hermetic re-gen of A4 emits
   `route-outcome("deal_list")` + `observer.pushedNames` assertion; no
   Text-widget flattening. Committed test green.
2. **findsNothing for hides** — hermetic re-gen of A5 emits
   `absence("t.auth.error")` + `findsNothing` + the mount guard.
   Committed test green.
3. **All 5 kinds traced** — the real `zfa tdd verify` run (this session,
   594s mutation phase) reports
   `presence=2 absence=1 route-outcome=2 enabled-state=1 sequence=1`;
   fresh verification.md regenerated by the run; restoration verified
   over all 10 registered subjects.
4. **LocaleTests + German pump** — slang shell resolves keys in
   A3/A5/A7; the `de` expansion pumps pass at 318–364% of EN anchor
   length; full 10-file scope `+12: All tests passed!`.

## The regression this lane fixed

Pristine master: `zfa tdd verify --feature 004-login-ui` →
`gate: preflight_red` — the #1393 re-split's deferred U1 green left the
registry's 10-behavior preflight red at `u1_test.dart`. This lane greens
U1 (declared adaptive_slots, Skin Contract order) with the red→green
cycle hash-chained in the feature cycle-log, then re-runs the full audit.

## Analyze / format gates

```
dart analyze example/lib/tdd/004-login-ui/u1_subject.dart
→ No issues found!
dart format --output=none --set-exit-if-changed example/lib/tdd/004-login-ui/u1_subject.dart
→ Formatted 1 file (0 changed)
```

Mutation gate honestly NOT green: `fail_survived` with 9 documented
survivors (8 pre-existing baseline — login_view W1 view gaps + A-lane
equivalent-class — plus this lane's `u1_subject.dart:31` slot-literal
content mutant, unobservable by the type-level U1 test, reported with its
remediation hint). The epic's exit criteria demand the kind trace, which
passes; the survivor ledger ships honestly, as the Sep-9 baseline did.
