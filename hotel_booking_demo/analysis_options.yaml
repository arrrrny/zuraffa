# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

# The following line activates a set of recommended lints for Flutter apps,
# packages, and plugins designed to encourage good coding practices.
include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/generated/**"
    - "build/**"
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

linter:
  # Enhanced lint rules for Clean Architecture and code quality
  rules:
    # Style and formatting
    prefer_single_quotes: true
    require_trailing_commas: true
    sort_constructors_first: true
    sort_unnamed_constructors_first: true

    # Clean Architecture principles
    avoid_classes_with_only_static_members: false  # Allow for utility classes
    avoid_function_literals_in_foreach_calls: true
    avoid_setters_without_getters: true
    prefer_final_fields: true
    prefer_final_locals: true
    prefer_final_in_for_each: true

    # Type safety
    always_declare_return_types: true
    annotate_overrides: true
    avoid_dynamic_calls: true
    prefer_typing_uninitialized_variables: true

    # Performance and memory
    avoid_unnecessary_containers: true
    sized_box_for_whitespace: true
    use_decorated_box: true

    # Zuraffa specific (Result types, Clean Architecture)
    prefer_const_constructors: true
    prefer_const_literals_to_create_immutables: true

    # Documentation
    public_member_api_docs: false  # Set to true for published packages
