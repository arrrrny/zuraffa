// Generated by zfa for: Concert
// ignore_for_file: no_logic_in_create_state
import 'package:flutter/material.dart';
import 'package:zuraffa/zuraffa.dart';

import '../../../domain/entities/concert/concert.dart';
import 'concert_controller.dart';
import 'concert_presenter.dart';
import 'concert_state.dart';

class ConcertDetailView extends CleanView {
  const ConcertDetailView({
    super.key,
    super.routeObserver,
    this.concert,
    this.id,
  });

  final Concert? concert;

  final String? id;

  @override
  State<ConcertDetailView> createState() {
    return _ConcertDetailViewState(
      ConcertController(ConcertPresenter(), initialConcert: concert),
    );
  }
}

class _ConcertDetailViewState
    extends CleanViewState<ConcertDetailView, ConcertController, ConcertState> {
  _ConcertDetailViewState(super.controller);

  @override
  onInitState() {
    super.onInitState();
    widget.id != null ? controller.watchConcert(widget.id!) : null;
  }

  @override
  Widget get view {
    return Scaffold(
      key: globalKey,
      appBar: AppBar(title: const Text('Concert')),
      body: ControlledWidgetBuilder<ConcertController>(
        builder: (context, controller) {
          final ConcertState viewState = controller.viewState;
          return Container(key: ValueKey(viewState.hashCode));
        },
      ),
    );
  }
}
