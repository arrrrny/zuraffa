// Generated by zfa
// zfa generate Todo --methods=create --test

import 'package:flutter_test/flutter_test.dart';
import 'package:mocktail/mocktail.dart';
import 'package:example/src/domain/entities/todo/todo.dart';
import 'package:example/src/domain/repositories/todo_repository.dart';
import 'package:example/src/domain/usecases/todo/create_todo_usecase.dart';

class MockTodoRepository extends Mock implements TodoRepository {}
class MockTodo extends Mock implements Todo {}

void main() {
  late CreateTodoUseCase useCase;
  late MockTodoRepository mockRepository;

  setUp(() {
    
    mockRepository = MockTodoRepository();
    useCase = CreateTodoUseCase(mockRepository);
  });

  group('CreateTodoUseCase', () {
    final tTodo = MockTodo();
    
    

    test('should call repository.create and return result', () async {
      // Arrange
      when(() => mockRepository.create(any())).thenAnswer((_) async => tTodo);

      // Act
      final result = await useCase(tTodo);

      // Assert
      verify(() => mockRepository.create(any())).called(1);
      expect(result.isSuccess, true);
      expect(result.getOrElse(() => throw Exception()), equals(tTodo));
    });

    test('should return Failure when repository throws', () async {
      // Arrange
      final exception = Exception('Error');
      when(() => mockRepository.create(any())).thenThrow(exception);

      // Act
      final result = await useCase(tTodo);

      // Assert
      verify(() => mockRepository.create(any())).called(1);
      expect(result.isFailure, true);
    });
  });
}
